# # A61: グラフの表現 (隣接リスト)
#
# ## 概要
#
# N 頂点 M 辺の無向グラフを入力し、各頂点の隣接頂点を出力する。
#
# ## データ構造
#
# 隣接リスト G[i] = 頂点 i に隣接する頂点のリスト
#
# ## 計算量
#
# O(N + M)
#
# ## 読み方の地図
#
# - 問題の型: グラフ / 難易度目安: ★1
# - 要約: 隣接リスト形式で無向グラフを表現する基本。
# - まず入力ブロックで、どの変数・配列に何が入るかを確認する。
# - 次に中心となる配列・状態・データ構造の意味を 1 行で言語化する。
# - 最後に答えを出す式や出力ループだけを、入力例と照らして追う。
#
# ## このコードで特に見る場所
#
# - 入力をまず隣接リストやグリッドの隣接関係に変換している部分を見る。
# - visited や dist は「もう見たか」「何手で来たか」を記録するための配列。
# - DFS は深く進む、BFS は近い順に広がる。この違いだけ先に押さえる。

# --- 入力 -----------------------------------------------------------
N, M = map(int, input().split())
edges = [list(map(int, input().split())) for _ in range(M)]

# --- 隣接リストの構築 (1-indexed) -----------------------------------
G = [list() for _ in range(N + 1)]
for a, b in edges:
    # 無向グラフなので双方向に登録
    G[a].append(b)
    G[b].append(a)

# --- 出力 -----------------------------------------------------------
for i in range(1, N + 1):
    output = ''
    output += str(i)
    output += ': {'
    # 隣接頂点を ", " 区切りで連結
    output += ', '.join(map(str, G[i]))
    output += '}'
    print(output)
