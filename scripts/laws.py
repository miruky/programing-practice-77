"""鉄則本 77 法則 (A01〜A77) の詳細解説データ。

各法則は次のフィールドを持つ:
    title:    短い表題 (例: "1 次元 DP の基本")
    tagline:  サブタイトル (例: "選択肢の中で最も良いものを順に決めていく")
    intro:    導入文 (Markdown 可)
    sections: list[dict]
              - heading: 節タイトル
              - body:    Markdown 本文
              - diagram: mermaid 図 (省略可)
    pitfalls: 詰まりやすい点 (Markdown)
    related:  関連法則 ID のリスト

mermaid 図は ```mermaid``` フェンスで囲んで body 内に直接埋め込んでも、
個別の diagram フィールドに置いてもよい (build.py が両方扱える)。

C 問題は最も近い A 法則にマッピングする (build.py 側 LAW_FOR_C を参照)。
"""

# C 問題から最も近い A 法則へのマッピング
LAW_FOR_C = {
    "C01": "A01",  # 単純な入出力
    "C02": "A03",  # 二重ループ全探索
    "C03": "A06",  # 1 次元累積和
    "C04": "A26",  # 試し割り
    "C05": "A04",  # 進法変換
    "C06": "A61",  # グラフの構築
    "C07": "A11",  # 二分探索
    "C08": "A05",  # 多重ループ全探索
    "C09": "A16",  # 基本 1 次元 DP
    "C10": "A29",  # 繰り返し二乗法
    "C11": "A12",  # 答えで二分探索
    "C12": "A21",  # 区間 DP
    "C13": "A30",  # mod 逆元
    "C14": "A64",  # ダイクストラ
    "C15": "A39",  # 区間スケジューリング
    "C16": "A22",  # 経路 DP / DAG 上 DP
    "C17": "A52",  # キュー (双方向のデック)
    "C18": "A21",  # 区間 DP
    "C19": "A58",  # セグメント木
    "C20": "A48",  # 焼きなまし法
}


LAWS = {}


def L(law_id, title, tagline, intro, sections, pitfalls, related=None):
    """LAWS への登録ヘルパ。"""
    LAWS[law_id] = {
        "id": law_id,
        "title": title,
        "tagline": tagline,
        "intro": intro,
        "sections": sections,
        "pitfalls": pitfalls,
        "related": related or [],
    }


def S(heading, body, diagram=None):
    """セクション辞書のヘルパ。"""
    d = {"heading": heading, "body": body}
    if diagram:
        d["diagram"] = diagram
    return d


# =====================================================================
#  Chapter 1 — アルゴリズムと計算量 (A01〜A05)
# =====================================================================

L("A01",
  title="標準入出力 — プログラムの入口と出口",
  tagline="まず「読む・書く」だけの最小プログラムを書けるようになる",
  intro=(
      "競技プログラミングでは、入力を **標準入力** から受け取り、"
      "答えを **標準出力** に書き出します。"
      "Python では `input()` と `print()` がその窓口で、"
      "他のすべての法則はこの土台の上に乗っています。\n\n"
      "ここを「呼び出すだけの儀式」と捉えてしまうと、後で `int()` の付け忘れや、"
      "改行・空白の罠で詰まります。1 行ずつ "
      "**「どの変数に、どんな型で何が入るか」** を意識して書き始めましょう。"
  ),
  sections=[
      S("Step 1: 入力の流れを掴む",
        "`input()` は標準入力から 1 行を **文字列** として取得します。"
        "整数として使いたければ `int(input())` で変換します。"
        "複数の数字が空白区切りで並んだ行は `map(int, input().split())` で"
        "個別の整数列にできます。",
        diagram=(
            "flowchart LR\n"
            "  A[標準入力<br/>'5'] -->|input| B[\"'5' (str)\"]\n"
            "  B -->|int| C[5 int]\n"
            "  C --> D[計算]\n"
            "  D -->|print| E[標準出力<br/>'25']"
        )),
      S("Step 2: print の挙動",
        "`print(x)` は `x` を文字列化して書き出し、最後に改行を入れます。"
        "改行を入れたくない場合は `print(x, end='')` のように指定します。"
        "複数の値を空白区切りで並べる際は `print(a, b, c)` で十分です。",
        diagram=(
            "flowchart TB\n"
            "  P[print x] --> S[str x]\n"
            "  S --> O[stdout に書き込み]\n"
            "  O --> N[改行を追加]"
        )),
      S("Step 3: 計算は中間に挟むだけ",
        "「入力 → 計算 → 出力」の 3 段で必ず書けます。"
        "A01 は計算が `N * N` の 1 行だけですが、後の問題でも"
        "この 3 段の骨格は変わりません。"),
  ],
  pitfalls=(
      "- `int()` を忘れて文字列のまま掛け算しないように。`'5' * '5'` は型エラー。\n"
      "- `input()` を 2 回呼ぶと 2 行読みます。1 行に複数の値があるときは"
      "  `split()` で 1 回の `input()` から分解する。\n"
      "- 末尾の改行が出力ジャッジで `WA` の原因になることがあります。"
      "  `print()` は自動で改行を付けるので安心です。"
  ),
  related=["A02", "A03"])


L("A02",
  title="線形探索 — 端から順に全部見る",
  tagline="まずは全部見る。それで間に合うなら勝ち",
  intro=(
      "配列の中に目的の値があるかを知りたいとき、最も素朴な方法が **線形探索** です。"
      "「先頭から最後まで 1 個ずつ照合する」だけのアルゴリズムで、計算量は O(N)。\n\n"
      "二分探索やハッシュよりも遅いと思われがちですが、"
      "**`N` がせいぜい 10^6 程度なら線形探索で十分間に合う**ことが多く、"
      "余計なバグの心配もありません。「速さよりまず正しさ」を学ぶ第一歩です。"
  ),
  sections=[
      S("Step 1: 全部見る = O(N)",
        "配列の要素数 `N` 個を 1 回ずつ見るので、計算量は **O(N)**。"
        "N が 10^7 を超え始めると 1 秒に間に合わなくなるので、"
        "そのときに二分探索やハッシュへの切り替えを考えます。",
        diagram=(
            "flowchart LR\n"
            "  A0[A0] -->|一致?| A1[A1]\n"
            "  A1 -->|一致?| A2[A2]\n"
            "  A2 -->|一致?| A3[A3]\n"
            "  A3 -->|一致?| A4[A4]\n"
            "  A4 --> R{見つかった?}"
        )),
      S("Step 2: 「あるかどうか」と「位置」",
        "Python なら `X in A` で存在判定 (内部は線形探索)、"
        "`A.index(X)` で位置取得 (見つからないと例外)。"
        "競プロでは自前ループの方がフラグ管理・早期 return が直感的です。"),
      S("Step 3: 早期終了で平均を速くする",
        "見つかった瞬間 `break` すれば、最悪は変わらないが平均的に半分の計算量で済みます。"
        "ループ後に `else` を付けると「最後まで `break` されなかった」場合の処理が書けます。",
        diagram=(
            "flowchart TB\n"
            "  L[for x in A] --> C{x == X?}\n"
            "  C -- Yes --> Y[Yes と出力して break]\n"
            "  C -- No --> L\n"
            "  L -.最後まで no_break.-> N[No と出力]"
        )),
  ],
  pitfalls=(
      "- 「ある」しか調べないなら早期 `break` を入れる。性能と読みやすさの両得。\n"
      "- N が 10^7 を超えるなら、線形探索ではなく **二分探索 (A11)** や"
      "  辞書 (A54) に切り替えるサイン。\n"
      "- 入力に重複が含まれるかで、「最初に見つけた」「全部数える」の挙動が変わる。"
  ),
  related=["A11", "A54"])


L("A03",
  title="二重ループ全探索 — 2 つの軸を独立に試す",
  tagline="2 重の for 文だけで多くの問題が殴れる",
  intro=(
      "「2 つの集合からそれぞれ 1 つずつ選ぶ」「2 つのインデックスの組を試す」"
      "といった問題は、外ループと内ループの **二重 for** で書けます。"
      "計算量は O(N²) なので、N が 5000 くらいまでなら通用します。\n\n"
      "全探索は **「速さを犠牲にして、正解を確実に当てる」** 戦略です。"
      "工夫した解法に行く前に、「全探索なら何秒?」をまず見積もる習慣をつけましょう。"
  ),
  sections=[
      S("Step 1: O(N²) の感覚",
        "N=100 なら 10^4、N=1000 なら 10^6、N=5000 なら 2.5×10^7。"
        "Python では 10^7 操作で 1〜数秒なので、N=5000 がギリギリの目安。",
        diagram=(
            "flowchart LR\n"
            "  A[N=100<br/>10^4] --> B[一瞬]\n"
            "  C[N=1000<br/>10^6] --> D[余裕]\n"
            "  E[N=5000<br/>2.5x10^7] --> F[Python だと際どい]\n"
            "  G[N=10^5<br/>10^10] --> H[不可]"
        )),
      S("Step 2: 順序付きの組 vs 順不同",
        "(i, j) と (j, i) を区別するなら `for i in range(N): for j in range(N):`、"
        "区別しないなら内側を `for j in range(i+1, N):` にして"
        "**重複を半分にカット**します。",
        diagram=(
            "flowchart TB\n"
            "  A[2 つの添字を全部試したい] --> B{順序を区別する?}\n"
            "  B -- する --> C[i:0..N-1<br/>j:0..N-1]\n"
            "  B -- しない --> D[i:0..N-1<br/>j:i+1..N-1]"
        )),
      S("Step 3: 条件判定は内側で",
        "外ループで a、内ループで b を固定し、その組について「条件を満たすか」を"
        "if で判定。マッチしたらカウンタを上げる、答えを出して終了する、などの処理を入れます。"),
  ],
  pitfalls=(
      "- N=10^5 になると O(N²) = 10^10 で不可能。前処理や別の法則に切り替える。\n"
      "- 順不同の組を数えるとき、`j in range(N)` のままだと答えが 2 倍になる。\n"
      "- 二重 for の中で `print` を毎回呼ぶと I/O だけで遅くなる。答えは最後に 1 回。"
  ),
  related=["A02", "A05", "A42"])


L("A04",
  title="進法変換 — 10 進数と 2 進数を行き来する",
  tagline="数を「位」で分解する。コンピュータ世界の母国語",
  intro=(
      "10 進数の `5` は、2 進数では `101`、つまり `1·4 + 0·2 + 1·1` です。"
      "進法変換は、数を **位 (= 重み)** に分解して考える練習です。\n\n"
      "ビット演算 (A23, A70 など) や繰り返し二乗法 (A29) は、"
      "「数の 2 進数表記」を直接扱います。ここで進法の扱いに慣れておくと、"
      "後のビット系の問題が一気に読みやすくなります。"
  ),
  sections=[
      S("Step 1: 上位桁から見る",
        "10 桁 (= 2^9) で表すなら、まず `2^9 = 512` の桁を `N // 512 % 2` で取り出し、"
        "次に `2^8 = 256` の桁、… と上位から並べると 2 進数になります。",
        diagram=(
            "flowchart LR\n"
            "  N[N=5] --> A[\"N//8 %2 = 0\"]\n"
            "  N --> B[\"N//4 %2 = 1\"]\n"
            "  N --> C[\"N//2 %2 = 0\"]\n"
            "  N --> D[\"N//1 %2 = 1\"]\n"
            "  A & B & C & D --> R[0101]"
        )),
      S("Step 2: 下位桁から逆順に作る別解",
        "`N % 2` で最下位ビットを取り、`N //= 2` で右シフト、を繰り返して逆順に積む方法もあります。"
        "短く書けますが、出力時に **逆順に並べ替える** ステップが必要。",
        diagram=(
            "flowchart LR\n"
            "  N0[N=5] --> M0[5 mod 2 = 1]\n"
            "  M0 --> N1[N=2]\n"
            "  N1 --> M1[2 mod 2 = 0]\n"
            "  M1 --> N2[N=1]\n"
            "  N2 --> M2[1 mod 2 = 1]\n"
            "  M2 --> O[\"['1','0','1']  → 逆順 → 101\"]"
        )),
      S("Step 3: 2 進数文字列 → 整数",
        "逆方向の変換は `int(s, 2)` 一発。"
        "学習目的では `for c in s: x = x * 2 + int(c)` のように、"
        "「位を 1 つ進めるたびに 2 を掛ける」流儀で書けると進法の理解が深まります。"),
  ],
  pitfalls=(
      "- 「0 詰めの桁数」を間違えると WA。10 桁指定なら必ず 10 桁出す。\n"
      "- Python の `bin(N)` は `'0b101'` のように接頭辞が付くので `[2:]` で剥がす。\n"
      "- 文字 `'0'` と整数 `0` は別物。`int(c)` を入れ忘れない。"
  ),
  related=["A23", "A29", "A70"])


L("A05",
  title="三重ループ全探索 — O(N³) でもまだ戦える",
  tagline="3 つの軸を独立に試したいとき",
  intro=(
      "「3 つの数の組 (x, y, z) を試して、合計が K になるか?」のような問題は"
      "三重 for で書けます。計算量は O(N³)。"
      "N=100 で 10^6 なので余裕、N=500 で 1.25×10^8 で Python だと際どい、というイメージ。\n\n"
      "三重ループまで膨らむときは、しばしば 1 つの軸を "
      "**「2 つを決めれば 3 つ目は自動で決まる」** という考えで O(N²) に落とせます。"
      "全探索でまず通してから、無駄を削っていくのが安全。"
  ),
  sections=[
      S("Step 1: O(N³) で間に合うかを最初に見積もる",
        "N=100 で 10^6 (一瞬)、N=300 で 2.7×10^7 (やや遅い)、"
        "N=1000 で 10^9 (不可)。"
        "「N が小さい」と書いてあれば三重ループが想定解のことが多い。",
        diagram=(
            "flowchart LR\n"
            "  A[N=100<br/>10^6] --> B[OK]\n"
            "  C[N=300<br/>2.7x10^7] --> D[Python: 際どい]\n"
            "  E[N=1000<br/>10^9] --> F[不可]"
        )),
      S("Step 2: O(N²) に落とす発想",
        "x, y を決めた時点で `z = K - x - y` の 1 通りに固定できます。"
        "あとは `z` が有効か (集合に含まれるか) を `set` で O(1) チェックすれば全体 O(N²)。",
        diagram=(
            "flowchart LR\n"
            "  X[\"x を選ぶ O(N)\"] --> Y[\"y を選ぶ O(N)\"]\n"
            "  Y --> Z[\"z=K-x-y を計算\"]\n"
            "  Z --> S[\"set で存在判定 O(1)\"]"
        )),
      S("Step 3: 重複の数え方に注意",
        "(1,2,3) と (3,2,1) を区別するか? 同じ数を複数回使えるか? "
        "問題文を最初に読んで、ループ範囲を `i < j < k` のように絞るかを判断する。"),
  ],
  pitfalls=(
      "- 三重 for は深くなるほど **添字ミスが起きやすい**。手元で N=3 程度で必ず手計算。\n"
      "- 「i, j, k が異なる」を要求されているなら `i, j, k` 全部に差をつける条件を入れる。\n"
      "- O(N³) で間に合わないとわかった時点で、O(N²) や O(N log N) に落とせないか検討する。"
  ),
  related=["A03", "A11", "A14"])


# =====================================================================
#  Chapter 2 — 累積和 (A06〜A10)
# =====================================================================

L("A06",
  title="1 次元累積和 — 区間和を O(1) で答える",
  tagline="先に「先頭からの合計」を作っておけば、引き算で区間和が出る",
  intro=(
      "数列 `A[1..N]` に対して、**`S[i] = A[1] + A[2] + ... + A[i]`** を前計算しておけば、"
      "任意の区間和 `A[l] + ... + A[r]` は `S[r] - S[l-1]` で **O(1)** に求まります。\n\n"
      "「クエリの度に毎回足し算する O(Q · N)」を「前計算 O(N) + クエリ O(Q)」に圧縮する、"
      "競プロにおける最重要テクの 1 つ。"
      "**2 次元 (A08) / いもす法 (A07) / セグメント木 (A58)** など、"
      "ここを起点に多くの法則が広がっていきます。"
  ),
  sections=[
      S("Step 1: 番兵の 0 を置く",
        "`S[0] = 0` と定義しておくと、`l=1` のような端のケースでも"
        "`S[r] - S[l-1] = S[r] - S[0]` で同じ式で書けます。"
        "Python の配列は 0-indexed ですが、`S` だけは 1-indexed に揃えると本の式と一致して読みやすい。",
        diagram=(
            "flowchart LR\n"
            "  S0[\"S[0]=0\"] --> S1[\"S[1]=A1\"]\n"
            "  S1 --> S2[\"S[2]=A1+A2\"]\n"
            "  S2 --> S3[\"S[3]=A1+A2+A3\"]\n"
            "  S3 --> Sn[...]"
        )),
      S("Step 2: 区間和は「右端 − 左手前」",
        "`A[l] + ... + A[r] = S[r] - S[l-1]`。"
        "下の図のように、長いバーから短いバーを引いて差分を取るイメージで覚えると一発で覚えられます。",
        diagram=(
            "flowchart LR\n"
            "  SR[\"S[r] = A1+...+Ar\"] --> M[\"−\"]\n"
            "  SL[\"S[l-1] = A1+...+A(l-1)\"] --> M\n"
            "  M --> ANS[\"= Al + ... + Ar\"]"
        )),
      S("Step 3: 半開区間で書く流儀",
        "**半開区間 [l, r) (= r を含まない)** で考えると、`S[r] - S[l]` と式から `-1` が消えてズレが減ります。"
        "セグメント木 (A58) など他の法則とも整合しやすく、慣れたらこちらの記法もおすすめ。"),
  ],
  pitfalls=(
      "- `S[l-1]` の `-1` は閉区間 [l, r] 用。"
      "  問題が **半開区間** で来たら `S[r] - S[l]` に書き換える。\n"
      "- `l = 1` のとき `S[0]` を参照する。番兵を置いていないと IndexError。\n"
      "- 答えが大きくなる問題では `int` のオーバーフローを気にする必要はない (Python は任意精度)。"
  ),
  related=["A07", "A08", "A10", "A38"])


L("A07",
  title="いもす法 — 区間加算を端点だけで処理する",
  tagline="区間に +1 する代わりに、両端だけ +1 / −1 して最後に累積和",
  intro=(
      "区間 `[l, r]` に `+v` を加算するクエリが Q 個あって、最終的な各位置の値を知りたい。"
      "毎回区間全体を更新すると O(Q · N) ですが、**いもす法** を使うと O(Q + N)。\n\n"
      "発想は単純: 加算開始の `l` に `+v`、加算終了の次 `r+1` に `-v` の印だけ置く。"
      "全部置き終わってから 1 次元累積和を取れば、印が **面に広がって** 元の加算を再現できます。"
  ),
  sections=[
      S("Step 1: 印を置く",
        "差分配列 `D[1..N+1]` を 0 で初期化。各クエリ `[l, r] += v` について"
        "`D[l] += v`、`D[r+1] -= v` を実行するだけ。各クエリ O(1)。",
        diagram=(
            "flowchart LR\n"
            "  Q[\"クエリ +v, [l,r]\"] --> A[\"D[l] += v\"]\n"
            "  Q --> B[\"D[r+1] -= v\"]"
        )),
      S("Step 2: 累積和で「面」に戻す",
        "全クエリを処理した後、`D` の累積和を取れば、各位置の最終値が一気に求まる。",
        diagram=(
            "flowchart LR\n"
            "  D[差分配列 D] --> C[\"S[i] = S[i-1] + D[i]\"]\n"
            "  C --> R[各位置の最終値]"
        )),
      S("Step 3: 計算量",
        "Q クエリ + N 個の累積和 = **O(Q + N)**。素朴な O(Q · N) と比べ、"
        "N=Q=10^5 なら 10^10 → 2·10^5 まで一気に圧縮されます。"),
  ],
  pitfalls=(
      "- 右端の `-v` は `r+1` に置く。`r` に置くと **r 自身も加算が止まる** ことになり 1 個ずれる。\n"
      "- 配列サイズは `N+2` 取ると `D[r+1]` で IndexError を起こさない。\n"
      "- 区間が半開 `[l, r)` で来たら、`-v` は `r` に置く。問題文の区間記法をまず確認。"
  ),
  related=["A06", "A09"])


L("A08",
  title="2 次元累積和 — 矩形の和を O(1) で答える",
  tagline="左上から (i, j) までの合計を持っておけば、長方形は引き算 3 回で求まる",
  intro=(
      "1 次元累積和 (A06) の 2 次元版です。"
      "`Z[i][j] = 「左上 (1,1) から (i,j) までの合計」` を前計算しておけば、"
      "任意の矩形 (A,B)-(C,D) の和は **`Z[C][D] - Z[A-1][D] - Z[C][B-1] + Z[A-1][B-1]`** で O(1)。\n\n"
      "重要なのは「最後の `+Z[A-1][B-1]`」。これは **包除原理** で、"
      "2 回引かれてしまった左上の長方形を 1 回戻すための補正です。"
  ),
  sections=[
      S("Step 1: Z[i][j] の構築",
        "0 行目・0 列目は 0 (番兵) のまま残し、`Z[i][j] = X[i-1][j-1] + Z[i-1][j] + Z[i][j-1] - Z[i-1][j-1]` で埋める。"
        "「今のマス + 上 + 左 − 左上の重複」の式が、まさに小さい矩形の包除そのもの。",
        diagram=(
            "flowchart TB\n"
            "  TL[左上 Z i-1 j-1] --> SUB[\"−\"]\n"
            "  T[上 Z i-1 j] --> ADD[\"+\"]\n"
            "  L[左 Z i j-1] --> ADD\n"
            "  X[X i-1 j-1] --> ADD\n"
            "  ADD --> R[\"Z[i][j]\"]\n"
            "  SUB --> R"
        )),
      S("Step 2: 矩形の和",
        "矩形 (A,B)-(C,D) の和は次の図のように 4 つの長方形の組み合わせで表せます。"
        "「大きい長方形 − 上の余り − 左の余り + 左上の戻し」。",
        diagram=(
            "flowchart LR\n"
            "  Z_CD[\"Z[C][D]\"] --> R[\"答え\"]\n"
            "  Z_AD[\"−Z[A-1][D]\"] --> R\n"
            "  Z_CB[\"−Z[C][B-1]\"] --> R\n"
            "  Z_AB[\"+Z[A-1][B-1]\"] --> R"
        )),
      S("Step 3: 2 パスで作る別実装",
        "「横方向に累積 → 縦方向に累積」の 2 回パスでも同じ `Z` が得られます。"
        "包除の式を覚えるのが苦手なら、こちらの方が直感的に書ける場面もあります。",
        diagram=(
            "flowchart LR\n"
            "  X[元配列 X] -->|横累積| H[各行が前から累積された配列]\n"
            "  H -->|縦累積| Z[2 次元累積和 Z]"
        )),
  ],
  pitfalls=(
      "- 配列の境界 (0 行目・0 列目) を **必ず 0 で残す**。番兵がないとクエリ式の `-1` がマイナス添字になる。\n"
      "- 包除の `+Z[A-1][B-1]` を忘れがち。1 つでも符号を間違えると致命的。\n"
      "- 半開区間と閉区間の混在に注意。問題文の (A,B)-(C,D) が閉なら今回の式、半開なら `-1` をすべて取り除く。"
  ),
  related=["A06", "A09"])


L("A09",
  title="2 次元いもす法 — 矩形加算を 4 隅で表現",
  tagline="長方形を毎回塗らない。四隅に + / − を置いて累積和で面に戻す",
  intro=(
      "A07 の 2 次元版。複数の矩形加算をまとめて処理したいとき、"
      "**矩形を毎回塗る代わりに 4 隅に印だけ置き**、最後に 2 次元累積和を取ると一気に面が復元できます。\n\n"
      "1 回のクエリで 4 点更新だけ、最後の累積和も O(HW)。"
      "N=10^5 個の矩形を 10^3 × 10^3 のグリッドに加算するような問題が瞬時に解けます。"
  ),
  sections=[
      S("Step 1: 4 隅の符号付き加算",
        "矩形 (A,B)-(C,D) に +v するときの印の置き方は次の通り。",
        diagram=(
            "flowchart TB\n"
            "  AB[\"X[A][B] += v\"] --> R[四隅に印を置く]\n"
            "  AD[\"X[A][D+1] -= v\"] --> R\n"
            "  CB[\"X[C+1][B] -= v\"] --> R\n"
            "  CD[\"X[C+1][D+1] += v\"] --> R"
        )),
      S("Step 2: 横方向に累積",
        "印が「右に伸びる横帯」を 4 本作る。"
        "上 2 本 (符号 +、−) と下 2 本 (符号 −、+) があり、上下で打ち消しの準備が整う。",
        diagram=(
            "flowchart LR\n"
            "  A[+v / -v が左右に対] -->|累積| B[+v が l〜r に分布]"
        )),
      S("Step 3: 縦方向に累積",
        "縦累積を取ると、上下の符号が打ち消し合って、最終的に矩形 (A,B)-(C,D) だけに +v が残る。"
        "印 4 点 → 横累積 → 縦累積 で、面が復元されたことになる。",
        diagram=(
            "flowchart LR\n"
            "  A[横帯 4 本] -->|縦累積| B[\"矩形 A,B - C,D に +v\"]"
        )),
  ],
  pitfalls=(
      "- 配列サイズは余裕を持って `(H+2) × (W+2)` 取ると、`+1` のはみ出しを心配しなくて済む。\n"
      "- 順序: 4 点印 → 横累積 → 縦累積 (順は入れ替えても OK だが必ず両方やる)。\n"
      "- 半開区間で渡されたら、右端の `+1` を消す。問題文の区間記法は最初に確認。"
  ),
  related=["A07", "A08"])


L("A10",
  title="区間最大値クエリ — 前後 2 方向の累積最大値",
  tagline="累積和の「max 版」。両端から max を取れば真ん中の答えが出る",
  intro=(
      "「i 番目だけ抜いたときの最大値」「区間 [l, r] の最大値」のように、"
      "**累積和ではなく累積 max** を持つと O(1) で答えられる問題があります。\n\n"
      "前から累積 max を `L[i]`、後ろから累積 max を `R[i]` と持っておけば、"
      "`max(L[i-1], R[i+1])` のように 2 つの累積を合わせて答えが作れる。"
      "「累積和」「累積 max」「累積 GCD」など、**演算を取り替えるだけで同じ枠組み**が使えるのが本質。"
  ),
  sections=[
      S("Step 1: 累積 max を前計算",
        "`L[i] = max(A[1], ..., A[i])`、`R[i] = max(A[i], ..., A[N])`。"
        "それぞれ 1 回のパスで作れる。",
        diagram=(
            "flowchart LR\n"
            "  A[配列 A] --> L[L: 左からの累積 max]\n"
            "  A --> R[R: 右からの累積 max]"
        )),
      S("Step 2: 「i を抜く」答え",
        "i を除いた最大は `max(L[i-1], R[i+1])`。"
        "両端の場合は片方が空になるので注意 (番兵で `-∞` を置いておくとケース分けが消える)。",
        diagram=(
            "flowchart LR\n"
            "  LIM[\"L[i-1]: i より前の max\"] --> A[\"max\"]\n"
            "  RIM[\"R[i+1]: i より後の max\"] --> A\n"
            "  A --> ANS[i を抜いた最大値]"
        )),
      S("Step 3: 累積 ××× の応用",
        "累積 max / min / GCD / OR / AND など、**結合則を持つ演算なら何でも累積できる**。"
        "これがセグメント木 (A58, A59) の発想に直結する。"),
  ],
  pitfalls=(
      "- 番兵に `-∞` を置くと両端の場合分けが消える。Python なら `float('-inf')` または `-10**18`。\n"
      "- 累積 GCD は順番に関係なく結合できる (gcd 自体が結合則を持つ) ので OK。\n"
      "- 累積引き算は **結合則がない** のでこの枠組みは使えない。"
  ),
  related=["A06", "A58"])


# =====================================================================
#  Chapter 3 — 二分探索 (A11〜A15)
# =====================================================================

L("A11",
  title="二分探索 — ソート済み列の真ん中を狙う",
  tagline="毎回半分捨てれば、N=10^18 でも 60 回で答えに到達",
  intro=(
      "ソート済みの配列で「目的の値の位置」を探すとき、毎回真ん中と比較して "
      "**半分を捨てる** だけで、O(log N) で到達できます。\n\n"
      "N=10^6 でも 20 回、N=10^18 でも 60 回程度の比較で済む脅威的な速さ。"
      "Python では `bisect.bisect_left` を使えば 1 行ですが、"
      "**「NG と OK の境目を探す」** という考え方は次の A12 (答えで二分探索) で必須になります。"
  ),
  sections=[
      S("Step 1: ループ不変条件",
        "`left, right` は **答えがある可能性のある範囲** を表す。"
        "毎回 `mid = (left + right) // 2` で半分に分け、片側を捨てる。",
        diagram=(
            "flowchart LR\n"
            "  L[\"left\"] --> M[\"mid = (l+r)//2\"]\n"
            "  M --> R[\"right\"]\n"
            "  M --> J{\"A[mid] と target を比較\"}\n"
            "  J -- target が左 --> NL[\"right = mid\"]\n"
            "  J -- target が右 --> NR[\"left = mid + 1\"]"
        )),
      S("Step 2: bisect_left と bisect_right の違い",
        "`bisect_left(A, x)` は「x を入れてもソートが崩れない最も左の位置」、"
        "`bisect_right(A, x)` は「最も右の位置」。"
        "重複がある場合、左境界と右境界が変わるので使い分けが必要。",
        diagram=(
            "flowchart LR\n"
            "  A[\"配列: 1 2 3 3 3 5\"] --> L[bisect_left x=3 = 2]\n"
            "  A --> R[bisect_right x=3 = 5]\n"
            "  L --> D[\"差 = 3 の個数\"]\n"
            "  R --> D"
        )),
      S("Step 3: 自前実装テンプレ",
        "ライブラリに頼らない実装パターン:\n\n"
        "```python\n"
        "left, right = 0, N\n"
        "while left < right:\n"
        "    mid = (left + right) // 2\n"
        "    if A[mid] >= x:\n"
        "        right = mid\n"
        "    else:\n"
        "        left = mid + 1\n"
        "# left は『A[i] >= x となる最初の位置』\n"
        "```"),
  ],
  pitfalls=(
      "- **配列がソートされていない** と二分探索は機能しない。最初に `sort()` を忘れない。\n"
      "- `mid = (l + r) // 2` で `l == r-1` のときに `mid == l` となり、"
      "  `l = mid + 1` の代わりに `l = mid` を書いていると **無限ループ**。\n"
      "- `bisect_right - bisect_left` で出現回数を数えるのは超頻出イディオム。"
  ),
  related=["A12", "A14", "A15", "A77"])


L("A12",
  title="答えで二分探索 — 「答えが x 以下か」を直接判定",
  tagline="探すのは値じゃない。OK/NG の境目",
  intro=(
      "「最大値を最小化したい」「最小値を最大化したい」「ちょうど K にしたい」"
      "といった最適化問題は、**「答えが x 以下か?」という判定問題** に書き換えると"
      "二分探索で解けます。これを **「答えで二分探索」(めぐる式二分探索)** と呼びます。\n\n"
      "肝は **単調性** : x を大きくすると判定結果が NG → OK と "
      "**一方向にだけ変わる**ことが保証されている必要があります。"
      "判定関数 `check(x)` を 1 つ書けばよく、全探索や微妙な貪欲よりも安全。"
  ),
  sections=[
      S("Step 1: 単調性を確認",
        "「時間 x 以上あれば終わるか」「容量 x 以下に収まるか」のように、"
        "x が大きくなるほど判定が易しくなる (NG → OK と切り替わる) ことを確認。",
        diagram=(
            "flowchart LR\n"
            "  X1[x=1<br/>NG] --> X2[x=2<br/>NG]\n"
            "  X2 --> X3[x=3<br/>OK]\n"
            "  X3 --> X4[x=4<br/>OK]\n"
            "  X4 --> X5[x=5<br/>OK]"
        )),
      S("Step 2: check(x) を実装",
        "「x 分で K 枚印刷できる?」など、**「x が与えられたら Yes/No を返す関数」**を 1 つ書く。"
        "通常の解法 (貪欲、シミュレーションなど) より単純になることが多い。"),
      S("Step 3: 二分探索で境界を絞り込む",
        "`check(x)` が NG → OK に切り替わる **最小の x** を二分探索で見つける。",
        diagram=(
            "flowchart TB\n"
            "  S[\"left=1, right=10^9\"] --> L[while left < right]\n"
            "  L --> M[\"mid = (l+r)//2\"]\n"
            "  M --> C{check mid}\n"
            "  C -- NG --> NL[\"left = mid + 1\"]\n"
            "  C -- OK --> NR[\"right = mid\"]\n"
            "  NL --> L\n"
            "  NR --> L\n"
            "  L --> A[\"left が答え\"]"
        )),
  ],
  pitfalls=(
      "- 単調性がないと二分探索は使えない。「x を大きくしたのに NG に戻る」なら別の方法へ。\n"
      "- 整数二分探索なら `while left < right`、実数二分探索なら `for _ in range(200)`。\n"
      "- 「最大化」のときは `while left < right: mid = (l+r+1)//2; ...` と切り上げ版を使う"
      "  (A77 で使うテンプレ)。"
  ),
  related=["A11", "A77"])


L("A13",
  title="しゃくとり法 — 左右の窓を一方向に動かす",
  tagline="右端は戻らない。広げては縮めるだけで O(N)",
  intro=(
      "「条件を満たす連続区間の個数」「最長の連続区間」のような問題で、"
      "**左端と右端の 2 つのポインタを一方向にだけ進める** ことで全体 O(N) を達成する技法。\n\n"
      "二重 for で書くと O(N²) になるところを、「左端を 1 つ進めるたびに、右端は前回の続きから動く」"
      "という性質を利用して右端を **巻き戻さない**ことで、全体の右端の動きも合計 N 回に抑えます。"
  ),
  sections=[
      S("Step 1: 単調性が必要",
        "「左端 l のとき、条件を満たす最大の右端は R(l)」が **l について単調** であること。"
        "つまり l を進めると R(l) も進む (戻らない)。これがしゃくとり法の前提。",
        diagram=(
            "flowchart LR\n"
            "  L1[l=1] --> R1[R=4]\n"
            "  L2[l=2] --> R2[R=5]\n"
            "  L3[l=3] --> R3[R=5]\n"
            "  L4[l=4] --> R4[R=7]"
        )),
      S("Step 2: 窓を広げる",
        "右端 `r` を「条件を満たす限り」進める while ループを内側に書く。"
        "条件を破った時点で停止し、その時の `r-l` などを答えに加算。",
        diagram=(
            "flowchart LR\n"
            "  L[l を固定] --> W[while 条件 OK と r 範囲内]\n"
            "  W -->|r 進める| W\n"
            "  W -->|止まったら| C[r-l などを集計]"
        )),
      S("Step 3: 左端を進める",
        "次のターンで `l += 1`。**`r` は戻さない**。これが O(N) を達成する核心。",
        diagram=(
            "flowchart LR\n"
            "  S1[l=1, r=1..4] --> S2[l=2, r=4..5]\n"
            "  S2 --> S3[l=3, r=5..5]\n"
            "  S3 --> S4[l=4, r=5..7]"
        )),
  ],
  pitfalls=(
      "- 「条件を満たす最大の r」と「条件を破る最小の r」を取り違えると個数が 1 ずれる。\n"
      "- `r` を毎回 `l` に戻すと O(N²) になり、しゃくとりの意味がなくなる。\n"
      "- 区間和 / 区間 GCD / 区間 OR など、計算量 O(1) で更新できる量に向く。"
  ),
  related=["A06"])


L("A14",
  title="半分全列挙 — N 個を半分に分け、2^(N/2) で全列挙",
  tagline="2^40 は無理。2^20 × 2 なら間に合う",
  intro=(
      "N 個の要素から部分集合を選ぶ全探索は 2^N。N=40 だと 10^12 で完全に不可能。"
      "**半分に分けて 2^(N/2) ずつ全列挙し、片方を二分探索 / set で照合する** と"
      "計算量は O(2^(N/2) · N) に。\n\n"
      "N=40 でも 2^20 ≒ 10^6 × N で間に合う。"
      "「N=30〜40 で部分集合の最適化」と書いてあったら半分全列挙の出番。"
  ),
  sections=[
      S("Step 1: 半分に分ける",
        "前半 `L1 = A[0:N//2]`、後半 `L2 = A[N//2:]`。"
        "それぞれの部分集合の合計を `Sum1`, `Sum2` として全列挙 (各 2^(N/2) 個)。",
        diagram=(
            "flowchart LR\n"
            "  N[\"N 個\"] --> L1[前半: N/2 個]\n"
            "  N --> L2[後半: N/2 個]\n"
            "  L1 --> S1[Sum1: 2^N/2 通り]\n"
            "  L2 --> S2[Sum2: 2^N/2 通り]"
        )),
      S("Step 2: 一方をソート",
        "`Sum2` をソートしておくと、`Sum1[i] + Sum2[j] == K` を満たす `Sum2[j]` を"
        "二分探索で O(log) で見つけられる。",
        diagram=(
            "flowchart LR\n"
            "  S2[Sum2 をソート] --> B[\"各 Sum1 i に対し<br/>K-Sum1 i を二分探索\"]\n"
            "  B --> ANS[該当する組が存在すれば Yes]"
        )),
      S("Step 3: 計算量",
        "前計算 O(2^(N/2) · N)、照合 O(2^(N/2) · log)。"
        "全体 **O(2^(N/2) · N)**。N=40 で約 4·10^7 オーダー。"),
  ],
  pitfalls=(
      "- 部分集合の全列挙は `for mask in range(1<<n): ...` でビット全探索。"
      "  `mask >> j & 1` で j 番目を選ぶか判定。\n"
      "- 「ちょうど K」を探すなら `set` で O(1) 照合の方が簡単。\n"
      "- N が奇数のときは前半・後半のサイズが 1 違う。`Sum2` の方を大きめに取ると無難。"
  ),
  related=["A11", "A23"])


L("A15",
  title="座標圧縮 — 値を「順位」に置き換える",
  tagline="値の絶対サイズに依存しない処理に書き換える定石",
  intro=(
      "値が `10^9` まで取りうるが、要素数は `10^5` しかないとき、"
      "**配列のサイズが値に依存して大きくなりがちな問題** (BIT、セグ木、頻度配列…) を"
      "**「値 → 順位」** に置き換えて N サイズに圧縮するテクニック。\n\n"
      "ソートして重複を除けば順位が決まり、`bisect_left(sorted_unique, x)` で順位を引ける。"
      "**転倒数 (B59)** や **座標による分類** の前処理として頻出。"
  ),
  sections=[
      S("Step 1: 重複を除いてソート",
        "`B = sorted(set(A))` で、A の値を昇順かつ重複なしの順位対応表に変換。",
        diagram=(
            "flowchart LR\n"
            "  A[\"A = [50, 10, 30, 30, 80]\"] --> B[\"B = sorted set A = [10,30,50,80]\"]"
        )),
      S("Step 2: 順位に変換",
        "各 `A[i]` を `bisect_left(B, A[i])` で順位に変換。"
        "値 10 → 0, 30 → 1, 50 → 2, 80 → 3。",
        diagram=(
            "flowchart LR\n"
            "  A[\"A = [50, 10, 30, 30, 80]\"] --> R[\"順位 = [2, 0, 1, 1, 3]\"]"
        )),
      S("Step 3: 後続処理は順位で",
        "頻度配列の長さが `len(B)` で済む。"
        "BIT、セグメント木、累積和などが「値域に対して」ではなく「要素数に対して」のサイズで作れる。"),
  ],
  pitfalls=(
      "- 重複の扱い: `set` は重複を除く。重複を残したい場合は `sorted(A)` を使う。\n"
      "- 順位は 0-indexed か 1-indexed か。BIT 系は 1-indexed が多い。\n"
      "- 元の値が必要な場合は順位 → 値のテーブル `B` を残しておく。"
  ),
  related=["A11", "A59"])


# =====================================================================
#  Chapter 4 — 動的計画法 (A16〜A25)
# =====================================================================

L("A16",
  title="1 次元 DP の基本 — dp[i] の意味を 1 行で言える",
  tagline="状態 → 遷移 → 答えの 3 段で書く",
  intro=(
      "動的計画法 (DP) は **「途中までの最適解を覚えておき、それを使って次を求める」** 手法。"
      "1 次元 DP は「現在位置 i」だけを状態に持つ最も基本の形。\n\n"
      "成功の鍵は **`dp[i]` の意味を日本語 1 行で固定する** こと。"
      "「i 番目に到着するまでの最小コスト」「i 番目までで作れる最大価値」等、"
      "意味さえはっきりすれば、遷移式は問題文を絵にする作業に過ぎません。"
  ),
  sections=[
      S("Step 1: 状態の定義",
        "**`dp[i] = 部屋 i にたどり着くまでの最小コスト`** と定義する。"
        "この 1 行を書ければ、半分終わったも同然。",
        diagram=(
            "flowchart LR\n"
            "  D[\"dp[i] の意味\"] --> Q[\"日本語 1 行で言える?\"]\n"
            "  Q -- Yes --> N[次へ]\n"
            "  Q -- No --> R[問題文を読み直す]"
        )),
      S("Step 2: 初期状態",
        "**手で答えがわかる地点** だけ埋める。今回なら `dp[1] = 0`、`dp[2] = A[2]`。"
        "「スタート地点」「空集合」「1 個だけ」のような自明な状態に対応。",
        diagram=(
            "flowchart LR\n"
            "  S1[\"dp[1] = 0 (出発点)\"] --> S2[\"dp[2] = A[2] (1→2 確定)\"]"
        )),
      S("Step 3: 遷移式 (漸化式)",
        "「i に来る方法」を全部列挙して、最小値 (最大化問題なら最大値) を取る。\n\n"
        "今回は「1 個前から」と「2 個前から」の 2 通りなので "
        "`dp[i] = min(dp[i-1] + A[i], dp[i-2] + B[i])`。",
        diagram=(
            "flowchart LR\n"
            "  P1[\"dp[i-1] + A[i]\"] --> M[min]\n"
            "  P2[\"dp[i-2] + B[i]\"] --> M\n"
            "  M --> R[\"dp[i]\"]"
        )),
      S("Step 4: 答えの取り出し",
        "ゴールに対応する `dp[N]` がそのまま答え。"
        "問題によっては `max(dp)` や `dp[N][W]` のような 2 次元の値を見ることもある。"),
  ],
  pitfalls=(
      "- `dp[i]` の意味を **数式ではなく日本語で固定** する。意味が曖昧だと遷移が書けない。\n"
      "- 初期状態は **手で答えがわかる地点だけ**。それ以外は遷移に任せる。\n"
      "- 1-indexed と 0-indexed の混在に注意。本サイトは A の先頭にダミーを詰めて"
      "  1-indexed に揃えている (式が本と一致するため)。"
  ),
  related=["A17", "A22", "A25"])


L("A17",
  title="DP + 経路復元 — ゴールから戻って『どう来たか』を再構築",
  tagline="DP は答えを出す。経路復元はその答えをたどる",
  intro=(
      "DP で最適値が求まったあと、「**どの選択をして** その値に辿り着いたか」を答える問題。"
      "DP テーブルは最適値だけしか持っていないので、**逆方向にトレースバック** して経路を組み立てます。\n\n"
      "発想は単純: ゴールから「`dp[i] = dp[i-1] + cost` を満たすか?」を見て、"
      "満たせばその遷移で来たことが確定。"
      "DP の意味が固まっていれば、復元側の実装は遷移式の逆引きに過ぎません。"
  ),
  sections=[
      S("Step 1: 普通の DP を解く",
        "A16 と同じく、`dp[i] = min(dp[i-1]+A[i], dp[i-2]+B[i])` を完成させる。",
        diagram=(
            "flowchart LR\n"
            "  S[スタート 1] --> M[\"dp[1..N] を埋める\"]\n"
            "  M --> G[ゴール N に最適値]"
        )),
      S("Step 2: ゴールから逆向きに辿る",
        "現在位置 `Place = N` から始めて、「`dp[Place-1] + A[Place] == dp[Place]` なら 1 個前から来た」"
        "と判定して `Place -= 1` または `Place -= 2`。",
        diagram=(
            "flowchart TB\n"
            "  G[Place = N] --> C{1 個前から来たか?}\n"
            "  C -- Yes --> M1[Place -= 1]\n"
            "  C -- No --> M2[Place -= 2]\n"
            "  M1 --> L[\"答えに Place を記録\"]\n"
            "  M2 --> L\n"
            "  L --> G"
        )),
      S("Step 3: 順序を反転して出力",
        "ゴール → スタートの順に記録したリストを `reverse()` してスタート → ゴール順に並べ替え、"
        "そのまま出力。"),
  ],
  pitfalls=(
      "- 遷移が **同点 (両方の式が成り立つ)** のときの分岐順で結果が変わる場合がある。"
      "  問題文の指定 (辞書順最小など) を確認。\n"
      "- 復元中、`Place == 1` (スタート) に到達したら停止条件を入れる。\n"
      "- 復元方向 (ゴール→スタート) と出力方向 (スタート→ゴール) は逆。reverse 忘れに注意。"
  ),
  related=["A16", "A64"])


L("A18",
  title="部分和問題 — 「i 枚から合計 j 作れるか」の bool DP",
  tagline="数値を作れるかを True/False で広げていく",
  intro=(
      "N 枚のカードからいくつか選んで合計を S にできるか? を判定する典型問題。"
      "**`dp[i][j]` = i 枚目までから合計 j を作れるか** という 2 次元 bool DP で解けます。\n\n"
      "**「使う / 使わない」の 2 択** で全探索しても 2^N (= N=30 で 10^9)。"
      "DP に書き直すと O(NS) で、N=100, S=10^4 でも瞬時。"
      "**「DP は全探索の言い換え」** という感覚を掴むのに最適な問題です。"
  ),
  sections=[
      S("Step 1: 状態の定義",
        "**`dp[i][j] = 最初の i 枚から合計 j を作れるか`** (True / False)。"
        "状態数は (N+1) × (S+1) ≒ 10^6 まで現実的。",
        diagram=(
            "flowchart LR\n"
            "  D[\"dp[i][j]\"] --> Q[True / False]\n"
            "  Q -.意味.- M[\"i 枚目までで合計 j が作れる?\"]"
        )),
      S("Step 2: 初期状態",
        "**`dp[0][0] = True`** (0 枚から合計 0 は空和で作れる)、"
        "`dp[0][1..S] = False` (0 枚で正の合計は不可能)。"),
      S("Step 3: 遷移",
        "i 枚目のカードの値 `A[i-1]` について、**「使わない」と「使う」の OR**。\n\n"
        "- 使わない: `dp[i][j] = dp[i-1][j]`\n"
        "- 使う (j >= A[i-1]): `dp[i][j] |= dp[i-1][j - A[i-1]]`",
        diagram=(
            "flowchart LR\n"
            "  P1[\"dp[i-1][j] (使わない)\"] --> OR[OR]\n"
            "  P2[\"dp[i-1][j-A[i-1]] (使う)\"] --> OR\n"
            "  OR --> D[\"dp[i][j]\"]"
        )),
  ],
  pitfalls=(
      "- 「使う」遷移は `j >= A[i-1]` のときだけ。条件分岐を忘れない。\n"
      "- `dp[N][S]` を見て True なら Yes、False なら No。\n"
      "- 値の総和が大きいとメモリが厳しい。1 次元に圧縮する別解もある (`dp[j] |= dp[j - A[i-1]]`)。"
  ),
  related=["A19", "A23"])


L("A19",
  title="ナップサック問題 — 重さと価値の 2 次元 DP",
  tagline="DP の王様。重量制限のもとで価値を最大化",
  intro=(
      "N 個の品物 (重さ `w[i]`, 価値 `v[i]`) から、合計重さ W 以下で価値を最大化する選び方。"
      "**`dp[i][j] = i 個目までで合計重さ j のときの最大価値`** で解ける、"
      "DP の代表格。\n\n"
      "「使う / 使わない」の 2 択を毎要素について解くという構造は A18 と同じですが、"
      "**価値の最大化** という点で `max` を使う点が違うだけ。"
      "アイデアを 1 つ覚えれば、応用問題が 5 個解けるタイプの法則です。"
  ),
  sections=[
      S("Step 1: 状態の定義",
        "**`dp[i][j] = 最初の i 個から重さ合計 j を作れるときの最大価値`**。"
        "到達できない状態には `-∞` を入れて max が壊れないようにする。",
        diagram=(
            "flowchart LR\n"
            "  D[\"dp[i][j]\"] --> Q[最大価値]\n"
            "  Q -.到達不能なら.- I[\"-∞\"]"
        )),
      S("Step 2: 初期状態",
        "**`dp[0][0] = 0`** (0 個・重さ 0・価値 0 はスタート地点)。"
        "それ以外は `-∞`。"),
      S("Step 3: 遷移",
        "i 個目について、**使わない / 使う の max** を取る。\n\n"
        "- 使わない: `dp[i][j] = dp[i-1][j]`\n"
        "- 使う (j >= w[i]): `dp[i][j] = max(dp[i][j], dp[i-1][j - w[i]] + v[i])`",
        diagram=(
            "flowchart LR\n"
            "  P1[\"dp[i-1][j]\"] --> M[max]\n"
            "  P2[\"dp[i-1][j-w[i]] + v[i]\"] --> M\n"
            "  M --> D[\"dp[i][j]\"]"
        )),
      S("Step 4: 答えの取り出し",
        "**`max(dp[N])`** を取れば、重さ上限 W 以内の任意の重さに対する最大価値の最大値。"
        "重さ W ジャストではないので `max` を取る点に注意。"),
  ],
  pitfalls=(
      "- 重さ W や価値 V が大きすぎると O(NW) が辛い。`W >> 10^5` なら B19 のように**価値添字**に切り替える別解を検討。\n"
      "- 「1 個まで使える 0-1 ナップサック」と「何個でも使える完全ナップサック」は遷移が異なる。\n"
      "- 1 次元配列に圧縮するときは内側ループを **逆順** にしないと同じ品物を複数回使う遷移になる。"
  ),
  related=["A18", "B19"])


L("A20",
  title="LCS — 最長共通部分列",
  tagline="2 つの文字列の「共通骨格」を測る古典 DP",
  intro=(
      "2 つの文字列 S, T に共通して現れる部分列のうち、**最長のものの長さ** を求める。"
      "部分列は連続でなくてもよい (順序だけ保つ)。\n\n"
      "**`dp[i][j] = S[0..i) と T[0..j) の LCS 長`** の 2 次元 DP。"
      "「最後の文字が一致するか?」で遷移が 2 分岐するパターンは、**編集距離 (B20)** や **回文部分列 (B21)** にも共通する万能の型。"
  ),
  sections=[
      S("Step 1: 状態の定義",
        "**`dp[i][j] = S の先頭 i 文字と T の先頭 j 文字の LCS の長さ`**。"
        "初期状態は `dp[0][j] = dp[i][0] = 0` (空文字列との LCS は 0)。"),
      S("Step 2: 一致する場合の遷移",
        "`S[i-1] == T[j-1]` なら、**この文字を LCS に追加できる**。"
        "`dp[i][j] = dp[i-1][j-1] + 1`。",
        diagram=(
            "flowchart LR\n"
            "  P[\"dp[i-1][j-1]\"] --> A[\"+1\"]\n"
            "  A --> D[\"dp[i][j]\"]"
        )),
      S("Step 3: 一致しない場合",
        "S か T のどちらかを 1 文字捨てた状態の **max** を取る。"
        "`dp[i][j] = max(dp[i-1][j], dp[i][j-1])`。",
        diagram=(
            "flowchart LR\n"
            "  P1[\"dp[i-1][j] (S の最後を捨てる)\"] --> M[max]\n"
            "  P2[\"dp[i][j-1] (T の最後を捨てる)\"] --> M\n"
            "  M --> D[\"dp[i][j]\"]"
        )),
  ],
  pitfalls=(
      "- 「最長共通**部分列**」と「最長共通**部分文字列** (連続)」は別物。"
      "  部分文字列だと遷移が違う。\n"
      "- 0 行目・0 列目を 0 で初期化するだけで境界処理が消える。番兵の威力。\n"
      "- LCS そのもの (文字列) を復元するには A17 と同じ要領で逆向きに辿る。"
  ),
  related=["A17", "B20", "B21"])


L("A21",
  title="区間 DP — 区間 [l, r] を状態にする",
  tagline="小さい区間から大きい区間へ。LEN を外ループに置くのが定石",
  intro=(
      "「両端から処理して中央に向かう」「最後に切る場所を全部試す」"
      "といった構造の問題で、**区間 [l, r] を状態とする 2 次元 DP**。\n\n"
      "**区間長 LEN を外側でループ** するのが定石。"
      "小さい区間の答えが先に確定するように順序を制御する必要があるからです。"
      "回文 (B21)、切り方 (C18)、ブロック取り (A21) など応用の幅が広い。"
  ),
  sections=[
      S("Step 1: 状態の定義",
        "**`dp[l][r] = 区間 [l, r] に対する最適値`**。"
        "「ブロック取りなら累積スコア」「回文なら最長回文部分列長」など、"
        "問題ごとに意味は変わる。"),
      S("Step 2: ループの順序",
        "**区間長 LEN を外側、左端 l を内側**。これで「短い区間の `dp` を先に確定」できる。",
        diagram=(
            "flowchart LR\n"
            "  L1[LEN=1] --> L2[LEN=2]\n"
            "  L2 --> L3[LEN=3]\n"
            "  L3 --> LN[...]"
        )),
      S("Step 3: 遷移パターン",
        "両端を 1 つずつ縮める / 中央で 2 分割する / 端を取り外すなど、"
        "**「区間をどう分解するか」** で式が決まる。\n\n"
        "ブロック取りなら `dp[l][r] = max(dp[l-1][r] + score(l-1), dp[l][r+1] + score(r+1))`。",
        diagram=(
            "flowchart LR\n"
            "  D[\"dp[l][r]\"] --> A[\"dp[l-1][r] + 左端の得点\"]\n"
            "  D --> B[\"dp[l][r+1] + 右端の得点\"]"
        )),
  ],
  pitfalls=(
      "- 外ループは **必ず区間長** にする。l, r を独立にループすると `dp[l+1][r-1]` が未確定で参照失敗。\n"
      "- 中央で 2 分割するタイプ (C18) は内側の **分割点 k** も全探索する 3 重ループ → O(N³)。\n"
      "- 1-indexed と 0-indexed の混在に注意。本サイトは入力配列にダミーを詰めて 1-indexed に揃える流儀。"
  ),
  related=["B21", "C12", "C18"])


L("A22",
  title="すごろく型 DP — 前向き遷移で書く",
  tagline="自分の dp が決まったら、次の dp を更新する",
  intro=(
      "「現在位置 i から、1 個進む / 2 個進む / ...」のように"
      "**「自分から次に配る」** スタイルの DP は「前向き遷移」「push 型」と呼ばれます。\n\n"
      "A16 のように **「自分が受け取る」** スタイル (後ろ向き遷移、pull 型) と数学的に等価ですが、"
      "「次の遷移先が複数あって、それぞれを更新する」場合 (DAG 上の最長路など) は"
      "前向きの方が自然に書けます (C16 など)。"
  ),
  sections=[
      S("Step 1: 全マスを INF で初期化",
        "**到達不能 = INF** で初期化、`dp[0] = 0` だけスタート地点。",
        diagram=(
            "flowchart LR\n"
            "  S[\"dp[0] = 0\"] --> X[\"dp[1..N-1] = INF\"]"
        )),
      S("Step 2: 前向き遷移",
        "`dp[i]` が確定したら、`dp[i+1]` と `dp[i+2]` を更新。\n\n"
        "```python\n"
        "for i in range(N):\n"
        "    if i+1 < N: dp[i+1] = min(dp[i+1], dp[i] + A[i])\n"
        "    if i+2 < N: dp[i+2] = min(dp[i+2], dp[i] + B[i])\n"
        "```"),
      S("Step 3: 後ろ向きとの違い",
        "後ろ向き (A16): `dp[i] = min(dp[i-1]+A[i], dp[i-2]+B[i])`。"
        "前向き (A22): `dp[i+1] = min(dp[i+1], dp[i]+A[i])`。"
        "**どちらも同じ答えを返す**。書きやすい方を選ぶ。",
        diagram=(
            "flowchart LR\n"
            "  B[\"後ろ向き: 自分が貰う\"] -.等価.- F[\"前向き: 自分が配る\"]"
        )),
  ],
  pitfalls=(
      "- 前向き遷移は配列を `min` で更新する形になるため、**初期化を INF にしておく** ことが必須。\n"
      "- 添字のはみ出し (`i+2 >= N`) に注意。`if` で囲むか、配列を余分に取る。\n"
      "- グラフ上の DP (DAG 上最長路など) は前向きの方が書きやすい。"
  ),
  related=["A16", "C16"])


L("A23",
  title="bitDP — 集合を整数 1 個に圧縮",
  tagline="2^N 通りの集合を 0..2^N-1 の整数で表す",
  intro=(
      "「どの要素を選んだか」の集合 S を、**N ビットの整数** として扱う DP。"
      "状態数が 2^N (= N=20 で約 10^6) なので、**N が小さい全探索の代用品** として使います。\n\n"
      "TSP (巡回セールスマン) を O(2^N · N²) で解いたり (B23)、"
      "クーポン券問題 (A23) を解いたりするのが代表例。"
      "**ビット演算 (AND, OR, XOR, シフト)** に慣れるのが第一歩。"
  ),
  sections=[
      S("Step 1: ビットと集合の対応",
        "整数 `state` の `k` ビット目が 1 ⇔ 要素 k が集合に含まれる。"
        "`1 << k` で「k 番目だけが ON」の集合を作れる。",
        diagram=(
            "flowchart LR\n"
            "  S[\"state = 5 (=101 in binary)\"] --> A[bit0 = 1: 要素0 含む]\n"
            "  S --> B[bit1 = 0: 要素1 含まない]\n"
            "  S --> C[bit2 = 1: 要素2 含む]"
        )),
      S("Step 2: 集合演算",
        "- 追加: `state | (1 << k)`\n"
        "- 削除: `state & ~(1 << k)`\n"
        "- 反転: `state ^ (1 << k)`\n"
        "- 含む?: `(state >> k) & 1`\n"
        "- 和集合: `s1 | s2`\n"
        "- 共通: `s1 & s2`"),
      S("Step 3: DP の形",
        "**`dp[i][state] = i 枚目まで処理し、状態 state を作る最小コスト`** のような 2 次元 DP。"
        "クーポン問題なら state は「無料になっている品物の集合」。\n\n"
        "遷移: 「i 枚目を使わない → state そのまま」「i 枚目を使う → state | coupon_i に行く」。",
        diagram=(
            "flowchart LR\n"
            "  P[\"dp[i-1][state]\"] --> N1[\"dp[i][state] (使わない)\"]\n"
            "  P --> N2[\"dp[i][state | coupon_i] + 1 (使う)\"]"
        )),
  ],
  pitfalls=(
      "- 状態数は 2^N。N=20 が現実的な上限 (約 10^6)。N=25 までなら工夫次第。\n"
      "- ループ順は `for state in range(1 << N)` で 0 から大きくしていくのが典型。\n"
      "- `state | next_state` で集合を結合できる。これが bitDP の万能ツール。"
  ),
  related=["A18", "B23", "A70"])


L("A24",
  title="LIS — 最長増加部分列",
  tagline="O(N log N) で部分列の最長を求める伝説のアルゴリズム",
  intro=(
      "配列 A から、**狭義に増加する** 部分列 (連続でなくてよい) のうち最長のものの長さ。"
      "素朴な DP は O(N²) ですが、**二分探索を使うと O(N log N)** に。\n\n"
      "「`L[k] = 長さ k+1 の増加部分列の最後の値の最小値`」という配列を作りながら走査する、"
      "**情報を圧縮した DP** の傑作。一見魔法に見えますが、`L` を保ち続けるだけ。"
  ),
  sections=[
      S("Step 1: L の意味",
        "**`L[k] = 長さ k+1 の増加部分列を作るときの『最後の値の最小値』**。"
        "短い部分列ほど、最後の値が小さい方が後で伸ばしやすい。",
        diagram=(
            "flowchart LR\n"
            "  A[\"A = [3, 1, 4, 1, 5, 9, 2, 6]\"] --> L[\"L = [1, 2, 6, 9, ...]\"]\n"
            "  L --> M[\"L[k] = 長さ k+1 のときの最後の値 min\"]"
        )),
      S("Step 2: 走査と更新",
        "各 `A[i]` について、`L` の中で「`A[i]` が入る位置」を **`bisect_left`** で求める。\n\n"
        "- 末尾なら append (LIS が 1 伸びる)\n"
        "- そうでなければ `L[pos] = A[i]` で更新 (より小さい値で塗り替える)",
        diagram=(
            "flowchart TB\n"
            "  X[\"A[i]\"] --> P[\"pos = bisect_left(L, A[i])\"]\n"
            "  P --> J{pos == len L?}\n"
            "  J -- Yes --> AP[\"L.append(A[i])\"]\n"
            "  J -- No --> UP[\"L[pos] = A[i]\"]"
        )),
      S("Step 3: 答えは len(L)",
        "走査終了時点での `len(L)` が LIS の長さ。"
        "**`L` 自体は LIS そのものではない** ことに注意 (各位置で違う LIS を「上書き」しているため)。"),
  ],
  pitfalls=(
      "- `L` の中身は LIS の実体ではない。長さを取り出すだけで、復元には別途記録が必要。\n"
      "- 「**真に**増加」と「単調増加 (=同じ値も OK)」で `bisect_left` / `bisect_right` を使い分け。\n"
      "- 同じ長さでも様々な LIS がある。「いずれか 1 つを出力」なら復元時に最後の選択を保持。"
  ),
  related=["A11", "A24_withoutDP", "B24"])


L("A25",
  title="経路数え上げ — 2 次元 DP で数える典型",
  tagline="格子上の経路数。障害物を `0` で消すだけ",
  intro=(
      "H × W の格子で、左上から右下まで「右」「下」だけで移動する経路数を求める。"
      "**`dp[i][j] = (0,0) から (i,j) に来る経路数`** という最も素直な 2 次元 DP。\n\n"
      "遷移は **`dp[i][j] = dp[i-1][j] + dp[i][j-1]`**。"
      "障害物 `#` のマスは `dp = 0` のまま残せばよく、「通れないなら 0 になる」という構造の美しさが光る。"
  ),
  sections=[
      S("Step 1: スタート地点を 1",
        "**`dp[0][0] = 1`** (スタート地点に来る方法は 1 通り、つまり最初からそこにいる)。"
        "他は `0` で初期化しておけば、障害物の処理が「触らない」だけで済む。"),
      S("Step 2: 遷移",
        "「上から来る」+「左から来る」の和。\n\n"
        "**`dp[i][j] = dp[i-1][j] + dp[i][j-1]`** (障害物なら `0` のまま)。",
        diagram=(
            "flowchart LR\n"
            "  U[\"dp[i-1][j] (上から)\"] --> P[+]\n"
            "  L[\"dp[i][j-1] (左から)\"] --> P\n"
            "  P --> D[\"dp[i][j]\"]"
        )),
      S("Step 3: 障害物 / 端の処理",
        "障害物 `#` のマスは初期値 `0` のまま残す。"
        "端 (i=0 or j=0) は番兵を `0` で取っておけば `dp[-1][j]` などの参照を気にしなくてよい。"),
  ],
  pitfalls=(
      "- 経路数は **mod 10^9+7** で答えさせる場合が多い。最初に modulo の必要性を確認。\n"
      "- スタートまたはゴールが障害物の問題も稀にある。`dp[0][0]` の初期化を慎重に。\n"
      "- メモリが厳しいなら 1 行分の配列で in-place 更新できる (典型 1D 圧縮)。"
  ),
  related=["A16", "A30", "B30"])


# =====================================================================
#  Chapter 5 — 数学的問題 (A26〜A35)
# =====================================================================

L("A26",
  title="素数判定 — √N までの試し割り",
  tagline="N の約数は必ず √N までに 1 つは入っている",
  intro=(
      "`N` が素数かを判定するには、**2 から √N までの整数で割れるか** を試すだけ。"
      "計算量 **O(√N)**、N=10^12 でも 10^6 回程度の試し割りで判定できます。\n\n"
      "鍵は **「N の約数の組 (a, b) (a≤b) は a ≤ √N」** という性質。"
      "片方を √N まで探せば、もう片方は自動で見つかる。"
      "約数列挙 (C04) やエラトステネス (B26) もこの性質の応用です。"
  ),
  sections=[
      S("Step 1: なぜ √N まででよいか",
        "N = a × b (a, b は整数, a ≤ b) なら、a² ≤ N、つまり a ≤ √N。"
        "なので a を √N まで探せば、ペアの b は自動で対応する数になる。",
        diagram=(
            "flowchart LR\n"
            "  N[\"N = a × b\"] --> C[\"a ≤ b\"]\n"
            "  C --> R[\"a² ≤ N\"]\n"
            "  R --> S[\"a ≤ √N\"]"
        )),
      S("Step 2: 試し割り",
        "`for i in range(2, int(N**0.5) + 1): if N % i == 0: 素数でない`。"
        "途中で 1 つでも割り切れたら合成数。",
        diagram=(
            "flowchart TB\n"
            "  L[\"i = 2, 3, ..., √N\"] --> C{N mod i == 0?}\n"
            "  C -- Yes --> NO[合成数]\n"
            "  C -- No --> L\n"
            "  L --> P[素数]"
        )),
      S("Step 3: 例外処理",
        "**N=1 は素数ではない**。`if N < 2: return False` を入れておく。"
        "`int(N**0.5)` は浮動小数の誤差で 1 ずれることがある。`int(N**0.5) + 1` で安全側に倒す。"),
  ],
  pitfalls=(
      "- N=1 を素数と判定しないように。\n"
      "- 浮動小数の `N**0.5` は誤差を含む。`isqrt(N)` (Python 3.8+) を使うと厳密。\n"
      "- 「N 以下の素数を全部欲しい」なら **エラトステネスのふるい (B26)** に切り替え。"
  ),
  related=["B26", "C04"])


L("A27",
  title="ユークリッド互除法 — 最大公約数を O(log) で",
  tagline="`gcd(a, b) = gcd(b, a mod b)` を繰り返すだけ",
  intro=(
      "2 つの整数の最大公約数 (GCD) を **`gcd(a, b) = gcd(b, a mod b)`** という"
      "シンプルな漸化式で求める、紀元前のアルゴリズム。"
      "計算量は **O(log min(a, b))**、a, b が 10^18 でも 90 回程度で終わります。\n\n"
      "**LCM (最小公倍数)** は `a * b // gcd(a, b)`、"
      "**3 個以上の GCD** は順に取れば求まる。"
      "Python なら `math.gcd` で 1 行ですが、原理を 1 回手で書いて理解しておくと"
      "拡張ユークリッド互除法・mod 逆元 (A30) に繋がります。"
  ),
  sections=[
      S("Step 1: 漸化式",
        "「a を b で割った余り = a mod b」は、a と同じ約数を b と共有する。"
        "なので gcd(a, b) = gcd(b, a mod b) が成り立つ。",
        diagram=(
            "flowchart LR\n"
            "  G1[gcd 24 18] --> G2[gcd 18 6]\n"
            "  G2 --> G3[gcd 6 0]\n"
            "  G3 --> A[\"答え = 6\"]"
        )),
      S("Step 2: 停止条件",
        "b が 0 になったとき、a が答え。\n\n"
        "```python\n"
        "def gcd(a, b):\n"
        "    while b != 0:\n"
        "        a, b = b, a % b\n"
        "    return a\n"
        "```"),
      S("Step 3: LCM への応用",
        "`lcm(a, b) = a * b // gcd(a, b)`。"
        "3 個以上の LCM は `lcm(lcm(a, b), c)` のように畳み込む。\n\n"
        "**先に gcd で割ってから掛ける** とオーバーフロー回避になる (Python では不要)。"),
  ],
  pitfalls=(
      "- a, b のどちらかが 0 のときの挙動: `gcd(a, 0) = a` (Python の `math.gcd` も同じ)。\n"
      "- LCM は値が爆発する。3 個以上の問題で `int` 範囲を超えるかチェック (Python なら任意精度)。\n"
      "- `a, b = b, a % b` の同時代入を `a = b; b = a % b` と分けると順序ミス。"
  ),
  related=["B27", "A30"])


L("A28",
  title="mod 計算 — 毎回余りを取って小さく保つ",
  tagline="加減乗は mod の中でも成り立つ。除算だけは別扱い",
  intro=(
      "答えが指数的に大きくなる問題で **「mod 10^9+7 で出力せよ」** と言われる典型。"
      "**`(a + b) % M = ((a % M) + (b % M)) % M`** などの性質を使い、"
      "毎回 mod を取って値を小さく保ちます。\n\n"
      "C 言語ならオーバーフロー対策、Python なら高速化の目的が大きい。"
      "**除算だけは mod 逆元 (A30)** が必要になる別扱いの操作。"
  ),
  sections=[
      S("Step 1: 加減乗は mod の中で OK",
        "**`(a ± b) % M = ((a % M) ± (b % M)) % M`**\n\n"
        "**`(a × b) % M = ((a % M) × (b % M)) % M`**\n\n"
        "計算の途中で `% M` を挟んでも結果は変わらない。",
        diagram=(
            "flowchart LR\n"
            "  A[a] --> P[\"× b\"]\n"
            "  P --> M[\"% M\"]\n"
            "  M -.同じ.- A2[\"(a % M) × (b % M) % M\"]"
        )),
      S("Step 2: 引き算の符号",
        "**`(a - b) % M`** は b > a のとき負になる。"
        "Python は **負の値でも 0 以上の余り** を返すので OK、C/C++ では `+ M` を入れて補正する流儀。"),
      S("Step 3: 除算は別法則",
        "通常の `(a // b) % M` は成り立たない。"
        "**mod 逆元 (A30)** を使って `a * inv(b) % M` と書き直す。"),
  ],
  pitfalls=(
      "- 加減乗の mod は **掛け算の前後** に挟むだけで OK。\n"
      "- 引き算で負になる Python と C/C++ の挙動の違いを意識。\n"
      "- 「mod 10^9+7」は素数なので、フェルマーの小定理で逆元が作れる (A30 で活用)。"
  ),
  related=["A29", "A30", "B28"])


L("A29",
  title="繰り返し二乗法 — a^b を O(log b) で",
  tagline="b を 2 進法で分解して、必要なベキだけ掛ける",
  intro=(
      "`a^b mod M` を求めるとき、b 回掛け算すると O(b) = 10^9 で論外。"
      "**b を 2 進数表記** にして、「bit が 1 の位だけ掛ける」と **O(log b)** で済みます。\n\n"
      "Python の `pow(a, b, M)` は内部でこれを使った専用実装で **C 並みに速い**。"
      "アルゴリズムを 1 回手で書いてから、本番では `pow(a, b, M)` を使うのが定石。"
  ),
  sections=[
      S("Step 1: 2 進数で分解",
        "`b = 13 = 1101_2 = 8 + 4 + 1`、"
        "つまり `a^13 = a^8 × a^4 × a^1`。",
        diagram=(
            "flowchart LR\n"
            "  B[\"b = 13 = 1101_2\"] --> A[\"a^13 = a^8 × a^4 × a^1\"]"
        )),
      S("Step 2: べき乗を二乗で作る",
        "`a^1, a^2, a^4, a^8, ...` は前の値を **二乗** すれば得られる。"
        "1 回の二乗で次のべき指数が倍々に増えていく。",
        diagram=(
            "flowchart LR\n"
            "  A1[\"a^1\"] -->|二乗| A2[\"a^2\"]\n"
            "  A2 -->|二乗| A4[\"a^4\"]\n"
            "  A4 -->|二乗| A8[\"a^8\"]"
        )),
      S("Step 3: 必要な位だけ答えに掛ける",
        "b の bit が 1 の位 (上の例なら 8, 4, 1) に対応するベキを答えに掛ける。"
        "`pow(a, b, M)` がやっているのは、これと等価な処理。"),
  ],
  pitfalls=(
      "- `M` を渡し忘れて `pow(a, b)` だけだと巨大整数になり遅くなる。**mod を必ず渡す**。\n"
      "- a の符号や 0 のケースに気を付ける。`a=0, b=0` の挙動は問題によって異なる。\n"
      "- 「フィボナッチ数の K 番目」「グラフの K 乗の遷移」など、**行列ベキ** へ拡張できる発想の元。"
  ),
  related=["A28", "A30", "B29"])


L("A30",
  title="mod 逆元 — フェルマーの小定理で割り算を作る",
  tagline="素数 M なら b⁻¹ ≡ b^(M-2) (mod M)",
  intro=(
      "mod の世界では普通の割り算 `a / b` は使えない。"
      "代わりに **`b` の mod 逆元 `b⁻¹` を掛ける**: `a × b⁻¹ ≡ a / b (mod M)`。\n\n"
      "M が素数なら **フェルマーの小定理** から `b⁻¹ ≡ b^(M-2) (mod M)`。"
      "繰り返し二乗法 (A29) で O(log M) で計算できる。"
      "**`pow(b, M-2, M)`** の 1 行で出てきます。"
  ),
  sections=[
      S("Step 1: 逆元の定義",
        "`b × b⁻¹ ≡ 1 (mod M)` を満たす `b⁻¹`。"
        "M が素数で b が M の倍数でなければ必ず存在する。",
        diagram=(
            "flowchart LR\n"
            "  B[b] -->|× b⁻¹ mod M| O[\"1 (mod M)\"]"
        )),
      S("Step 2: フェルマーの小定理",
        "**M が素数、b が M の倍数でないとき、`b^(M-1) ≡ 1 (mod M)`**。\n\n"
        "両辺を b で割れば `b^(M-2) ≡ b⁻¹ (mod M)`。"
        "Python の `pow(b, M-2, M)` でそのまま計算可能。"),
      S("Step 3: 二項係数への応用",
        "`nCr = n! / (r! × (n-r)!)` を mod で計算するには、"
        "分母を逆元で掛ける形に直す:\n\n"
        "**`nCr mod M = n! × (r!)⁻¹ × ((n-r)!)⁻¹ mod M`**\n\n"
        "階乗とその逆元を前計算しておけば各クエリ O(1)。",
        diagram=(
            "flowchart LR\n"
            "  N[n!] --> X[\"× r!⁻¹\"]\n"
            "  X --> Y[\"× (n-r)!⁻¹\"]\n"
            "  Y --> R[nCr mod M]"
        )),
  ],
  pitfalls=(
      "- M が **素数でない** と、フェルマーの小定理は使えない (拡張ユークリッド互除法を使う)。\n"
      "- b が M の倍数のときは逆元が存在しない (= 0 で割っているのと同じ)。\n"
      "- 階乗を毎回計算すると遅い。**`fact` と `inv_fact` を前計算** しておくと O(N) 前計算 + クエリ O(1)。"
  ),
  related=["A28", "A29", "B30", "C13"])


L("A31",
  title="包除原理 — 3 または 5 の倍数を数える型",
  tagline="A ∪ B = A + B − A ∩ B。重なりは 1 回だけ引く",
  intro=(
      "「3 の倍数または 5 の倍数」のような **「または条件」** を数えるとき、"
      "単純に「3 の倍数 + 5 の倍数」とすると、15 の倍数を **二重カウント** してしまう。\n\n"
      "包除原理は `|A ∪ B| = |A| + |B| − |A ∩ B|` という公式を使い、"
      "二重に数えた分を 1 回引いて補正する考え方。"
      "**集合 3 個以上は符号を交互に + / − で増やす** 一般形に拡張できます。"
  ),
  sections=[
      S("Step 1: 2 集合の包除",
        "`|A ∪ B| = |A| + |B| − |A ∩ B|`。"
        "3 の倍数の個数を A、5 の倍数の個数を B、15 の倍数の個数を A∩B とすると、"
        "「3 か 5 の倍数」は A + B − A∩B。",
        diagram=(
            "flowchart LR\n"
            "  A[3 の倍数] --> S[\"|A| + |B|\"]\n"
            "  B[5 の倍数] --> S\n"
            "  AB[15 の倍数] --> M[\"−|A∩B|\"]\n"
            "  S --> R[答え]\n"
            "  M --> R"
        )),
      S("Step 2: 3 集合の包除",
        "`|A ∪ B ∪ C| = |A|+|B|+|C| − |A∩B|−|B∩C|−|A∩C| + |A∩B∩C|`。"
        "**奇数集合は +、偶数集合は −** の符号則。",
        diagram=(
            "flowchart LR\n"
            "  S1[1 個ずつ: +] --> R[結果]\n"
            "  S2[2 個ずつ: -] --> R\n"
            "  S3[3 個全部: +] --> R"
        )),
      S("Step 3: 倍数の個数",
        "N 以下の `k` の倍数の個数は `N // k`。"
        "`A∩B` = lcm(3, 5) = 15 の倍数の個数 = `N // 15`。"
        "包除と LCM (A27) のコンボが基本。"),
  ],
  pitfalls=(
      "- 「`A∩B` は AかつB」と「`A∪B` は AまたはB」。日本語と記号を混同しない。\n"
      "- 3 集合以上は符号則を間違えやすい。要素数 k の組には符号 `(-1)^(k+1)`。\n"
      "- 「ちょうど 1 つ満たす」のような問題は、包除を入れ子で適用する応用形。"
  ),
  related=["A27", "B31"])


L("A32",
  title="ゲーム後退解析 — 終端から勝ち負けを伝播",
  tagline="勝てる状態 = 1 手で『相手が負ける状態』へ行ける",
  intro=(
      "**「相手と最善を尽くす 2 人ゲーム」** で勝敗を判定する DP。"
      "終端状態 (手番が来た時点で動けない = 負け) を起点に、"
      "**「1 手で負け状態に行ける状態 = 勝ち」「全ての手で相手が勝つ状態 = 負け」**"
      "を後ろから埋めていく。\n\n"
      "Nim (A33) や Grundy 数 (A34) は、この後退解析の上に成り立つ理論です。"
  ),
  sections=[
      S("Step 1: 終端を確定",
        "「動けない = 自分の負け」と問題ごとに決まる。"
        "終端は `dp[end] = False` (= 負け状態)。"),
      S("Step 2: 勝ち負けの定義",
        "- **勝ち状態**: 「1 手で **相手が負ける状態** へ移動できる」状態\n"
        "- **負け状態**: 「全ての遷移先が **相手が勝つ状態**」の状態",
        diagram=(
            "flowchart LR\n"
            "  S[状態 s] --> M{1 手で 負け状態に行ける?}\n"
            "  M -- Yes --> W[勝ち]\n"
            "  M -- No --> L[負け]"
        )),
      S("Step 3: 後ろから埋める",
        "終端 → その 1 手前 → 2 手前 … と逆順に埋めれば、全状態の勝敗が決まる。"
        "問題によっては DP テーブルではなく **メモ化再帰** で書くと自然な場合も多い。"),
  ],
  pitfalls=(
      "- 「自分の手番で動けない = 自分の負け」が定義の標準。"
      "  問題によっては「動けない = 自分の勝ち」のミゼールゲームもある。\n"
      "- ループのある状態空間は後退解析できない。DAG 的に整理できる必要がある。\n"
      "- 状態数が爆発する場合は **Grundy 数 (A34)** で独立成分に分解。"
  ),
  related=["A33", "A34", "A35"])


L("A33",
  title="Nim ゲーム — XOR が 0 でなければ先手必勝",
  tagline="複数山の Nim は XOR 和で勝敗が決まる",
  intro=(
      "**石の山が複数あり、各手番で 1 つの山から好きな数取れる** ゲーム (= Nim)。"
      "取れなくなった方が負け。\n\n"
      "驚くべき定理: **全山の石の数の XOR が 0 のとき、後手必勝**。"
      "1 山だけ取り出して試行錯誤するだけでは見えない、強力な数学的構造です。"
      "Grundy 数 (A34) はこれを「Nim でない一般ゲームを Nim に変換する」一般形。"
  ),
  sections=[
      S("Step 1: XOR の不思議な性質",
        "**XOR が 0 なら全部 0 か、ペアがある** (2 進数の各 bit で偶数個 1 が立っている)。"
        "1 つの数を変えると XOR が 0 ではなくなる。"
        "逆に、XOR が 0 でないとき、必ず「XOR を 0 にする手」が存在する。"),
      S("Step 2: XOR=0 なら後手必勝",
        "- **XOR=0 の状態**: どの山から何個取っても XOR は 0 でなくなる → 相手に「XOR≠0」を渡す\n"
        "- **XOR≠0 の状態**: 必ず「XOR を 0 にする手」がある → 相手に「XOR=0」を渡せる",
        diagram=(
            "flowchart LR\n"
            "  X0[XOR = 0] -->|何取っても| X1[XOR ≠ 0]\n"
            "  X1 -->|0 に戻す手あり| X0"
        )),
      S("Step 3: 実装",
        "全山の石数を XOR で集約するだけ。\n\n"
        "```python\n"
        "xor = 0\n"
        "for a in A:\n"
        "    xor ^= a\n"
        "print('First' if xor != 0 else 'Second')\n"
        "```"),
  ],
  pitfalls=(
      "- 「1 手で **最低 1 個** 取る必要があるか」「0 個も OK か」でルールが変わる。標準は **最低 1 個**。\n"
      "- ミゼール (最後に取った方が負け) は通常の Nim と勝敗が一部異なる。\n"
      "- 「2 次元 Nim」のような変種は、各座標の差を Nim 和に持ち込む発想 (B33)。"
  ),
  related=["A32", "A34", "B33"])


L("A34",
  title="Grundy 数 — 独立ゲームを Nim に変換する",
  tagline="各部分ゲームの Grundy 数の XOR が 0 なら後手必勝",
  intro=(
      "Nim 以外のゲームを **「Grundy 数」** という不変量に変換すると、"
      "**全部分ゲームの Grundy 数の XOR が 0 のとき後手必勝** という"
      "Nim と同じ判定法で勝敗が分かる、強力な理論。\n\n"
      "Grundy 数の定義: 「**遷移先の Grundy 数の集合に含まれない最小の非負整数 (mex)**」。"
      "再帰的に計算でき、終端状態は Grundy 数 0。"
      "小さい N で計算して **周期性を発見** することがよくあります。"
  ),
  sections=[
      S("Step 1: mex の計算",
        "`mex(S) = S に含まれない最小の非負整数`。"
        "`mex({0,1,3}) = 2`、`mex({1,2}) = 0`、`mex({}) = 0`。",
        diagram=(
            "flowchart LR\n"
            "  S[\"{0, 1, 3}\"] --> M[mex = 2]\n"
            "  T[\"{1, 2}\"] --> N[mex = 0]"
        )),
      S("Step 2: 1 山だけの Grundy 数",
        "「石が `a` 個あって、1〜k 個取れる」なら `Grundy(a) = a mod (k+1)`。"
        "周期 (k+1) で繰り返す。"
        "より複雑な取り方は **後退解析 + mex** で 1 つずつ計算する。",
        diagram=(
            "flowchart LR\n"
            "  G0[\"G(0)=0\"] --> G1[\"G(1)=1\"]\n"
            "  G1 --> G2[\"G(2)=2\"]\n"
            "  G2 --> G3[\"G(3)=0\"]\n"
            "  G3 --> G4[\"G(4)=1\"]"
        )),
      S("Step 3: 複数山の合成",
        "「N 個の独立ゲームの全体」の Grundy 数は **各ゲームの Grundy 数の XOR**。"
        "XOR が 0 なら後手必勝、それ以外は先手必勝。"),
  ],
  pitfalls=(
      "- 小さい N で **手計算 / プログラム計算で表を作り、周期を発見** するのが定石。\n"
      "- 「独立な部分ゲームに分解できる」ことが前提。盤面が絡み合うゲームには使えない。\n"
      "- Grundy 数を計算するだけのプログラムを別に用意して周期を見るとデバッグが楽。"
  ),
  related=["A32", "A33", "B34"])


L("A35",
  title="交互最善 DP — min と max を交互に切り替える",
  tagline="先手は max、後手は min。階層で交互に",
  intro=(
      "「2 人が交互に最善手を打つ」ゲームで、**先手は自分の得を最大化、後手は先手の得を最小化**"
      "するときの最終スコアを求める DP。\n\n"
      "**奇数階層では max、偶数階層では min** を取るだけ。"
      "後退解析 (A32) の応用形ですが、勝敗ではなく **スコア** を扱うので、"
      "min/max を交互に使う点が特徴。"
  ),
  sections=[
      S("Step 1: 状態の定義",
        "**`dp[i][j] = ピラミッドの (i, j) からゲームを開始したときの最終スコア`** とする。"
        "「先手の累計スコア」と読み替えてもよい。"),
      S("Step 2: 最下段は値そのもの",
        "ピラミッドの最下段 (i = N) は、その値がそのまま最終スコア。"
        "`dp[N][j] = A[j-1]`。"),
      S("Step 3: 上段は子の max / min",
        "**奇数段は先手 = max、偶数段は後手 = min**。\n\n"
        "```python\n"
        "if i % 2 == 1:  # 先手の手番\n"
        "    dp[i][j] = max(dp[i+1][j], dp[i+1][j+1])\n"
        "else:           # 後手の手番\n"
        "    dp[i][j] = min(dp[i+1][j], dp[i+1][j+1])\n"
        "```",
        diagram=(
            "flowchart TB\n"
            "  P[親 i j] --> C1[子 i+1 j]\n"
            "  P --> C2[子 i+1 j+1]\n"
            "  P -.奇数段 = max.- C1\n"
            "  P -.偶数段 = min.- C2"
        )),
  ],
  pitfalls=(
      "- 「先手」「後手」のどちらが max / min を取るかは問題文を慎重に読む。\n"
      "- 階層の偶奇でなく **手番数の偶奇** で切り替える問題もある。\n"
      "- 「先手のスコア − 後手のスコア」を最大化する問題は別の DP に書き換える (= 差分 DP)。"
  ),
  related=["A32", "B32"])


# =====================================================================
#  Chapter 6 — 考察テクニック (A36〜A45)
# =====================================================================

L("A36",
  title="偶奇の不変量 — Parity は変わらない",
  tagline="操作で「偶奇」が変わらないなら、その偶奇を見れば必ず答えが分かる",
  intro=(
      "「特定の操作を K 回行ったあとに、ある状態になれるか?」を問う問題で、"
      "**操作で変化しない量 (不変量)** を見つけられれば、最初と目標の不変量を比較するだけで判定できます。\n\n"
      "**偶奇 (parity)** は最も基本的な不変量。"
      "「2 マスずつ動く」「2 つの値を同時に増減」のような操作は偶奇を変えないことが多く、"
      "初期偶奇と目標偶奇が一致するかを見るだけで Yes/No が決まります。"
  ),
  sections=[
      S("Step 1: 不変量の発見",
        "操作を 1 回試してみて、**「何が必ず変わらないか?」** を観察。"
        "総和、偶奇、mod の値、要素数などが候補。",
        diagram=(
            "flowchart LR\n"
            "  S[初期状態] -->|操作| S2[状態 2]\n"
            "  S2 -->|操作| S3[状態 3]\n"
            "  S -.不変量 X.- I[X]\n"
            "  S2 -.不変量 X.- I\n"
            "  S3 -.不変量 X.- I"
        )),
      S("Step 2: 必要条件 vs 必要十分条件",
        "「不変量が一致しないなら No」は **必要条件**。"
        "「一致するなら本当に Yes か?」を別途確認する必要がある。\n\n"
        "A36 では「ON 個数の偶奇」が一致すれば必ず実現可能なことが示せる (= 必要十分)。"),
      S("Step 3: 実装は数行",
        "ON の個数を数えて、その偶奇と K の偶奇を比較。\n\n"
        "```python\n"
        "numON = S.count('1')\n"
        "print('Yes' if numON % 2 == K % 2 else 'No')\n"
        "```"),
  ],
  pitfalls=(
      "- 「偶奇が一致 = Yes」とは限らない。必要条件しか言えていない場合がある。\n"
      "- 不変量は **一意ではない**。複数見つかったらどれが本質的かを考える。\n"
      "- A45 のような「総和の不変性」も同じ枠組み。"
  ),
  related=["A45", "B36"])


L("A37",
  title="貢献度分解 — 各要素を独立に数える",
  tagline="「全部の和」を「各要素が答えに何回登場するか」で分解",
  intro=(
      "「ペアの和を全部足し合わせろ」「各組み合わせのコストの合計」"
      "といった集計問題で、**「個々の要素が答えに何回貢献するか」** を独立に計算してから合計する技法。\n\n"
      "二重ループで全ペアを足すと O(N²) ですが、"
      "**「i 番目の数は答えに何回足されるか?」** を直接式で出せれば一気に O(N) や O(N log N)。"
      "桁ごとの集計 (B37) や転倒数の解析にも応用が利く視点。"
  ),
  sections=[
      S("Step 1: 全部を 1 つの要素ごとに分解",
        "答え = Σ (各要素 × その要素が貢献する回数)。"
        "例: 全ペア和 Σ (a_i + a_j) なら、各 a_i は (N-1) 回足される → 答え = (N-1) × Σ a_i。",
        diagram=(
            "flowchart LR\n"
            "  S[全ペア和] --> D[\"a_i × (a_i が登場する回数)\"]\n"
            "  D --> SUM[各 i について合計]"
        )),
      S("Step 2: 「i 番目の貢献度」を式で出す",
        "問題によっては、要素 i の貢献回数が組合せ的に決まる (C(N-1, k-1) など)。"
        "貢献度の式さえ立てば、全体は **一巡 O(N)** で計算できる。"),
      S("Step 3: 桁ごと / 集合ごとへの一般化",
        "数値の桁ごとに「桁 d に数字 v が現れる回数」のように、**問題を独立な軸に分解** すると同じ枠組み (B37)。",
        diagram=(
            "flowchart LR\n"
            "  N[数値 N の桁和合計] --> D[各桁を独立に集計]\n"
            "  D --> S[\"Σ (桁 d に数字 v が現れる回数 × v)\"]"
        )),
  ],
  pitfalls=(
      "- 「i 番目の貢献回数」を **問題文の図** から導く。手で N=3〜5 を試すと法則が見える。\n"
      "- 重複が絡む場合は組合せの公式を慎重に。\n"
      "- 貢献度ベースの計算ができれば、大きな N でも一発で答えが出ることが多い。"
  ),
  related=["B37", "B40"])


L("A38",
  title="左右からの累積 — 各位置の上限を独立に求める",
  tagline="左から見た条件と右から見た条件、両方を満たす最大値",
  intro=(
      "「左から L[i] 以内」「右から R[i] 以内」のような **左右両方からの制約** が"
      "各位置にかかる問題は、**左右から累積最小 (または最大) を独立に計算してから min を取る** と O(N) で解ける。\n\n"
      "「上界」「実現可能なギリギリ」を求めるタイプの問題に頻出。"
      "累積和 (A06) の応用とも言える。"
  ),
  sections=[
      S("Step 1: 左からの最大連続長",
        "「i 番目までで連続する '<' の長さ」のような量を、左から 1 回パスで計算。"
        "`L[i] = L[i-1] + 1 (条件を満たすなら) else 1` のような形。",
        diagram=(
            "flowchart LR\n"
            "  L0[L 0] --> L1[L 1]\n"
            "  L1 --> L2[L 2]\n"
            "  L2 --> L3[L 3]"
        )),
      S("Step 2: 右からの最大連続長",
        "同じものを右から 1 回パス。`R[i]` を求める。",
        diagram=(
            "flowchart LR\n"
            "  R3[R 3] --> R2[R 2]\n"
            "  R2 --> R1[R 1]\n"
            "  R1 --> R0[R 0]"
        )),
      S("Step 3: 各位置の答えは max(L[i], R[i])",
        "問題によっては L[i] + R[i] − 1、min(L[i], R[i]) などになる。"
        "**前計算 O(N) + 答え O(N)** で、二重ループより圧倒的に高速。"),
  ],
  pitfalls=(
      "- 左右からのパスで、**端のマス** の扱い (L[0], R[N-1]) を間違えやすい。\n"
      "- max を取るのか、和を取るのか、min を取るのかは問題依存。\n"
      "- 「区間の上限を集計する」(A38 本問) と「上界を求めて最大化」(B38) で式が異なる。"
  ),
  related=["A06", "B38"])


L("A39",
  title="区間スケジューリング — 終了時刻でソート + 貪欲",
  tagline="終了が早いものから取れば、必ず最適",
  intro=(
      "N 個の区間から **重ならないように最大個数選ぶ** 古典問題。"
      "「**終了時刻で昇順ソートし、左から見て採用可能なら採用**」という"
      "貪欲法が **必ず最適** であることが証明されています。\n\n"
      "「他の貪欲基準 (開始時刻順、長さ順) はダメ」というのが意外な点。"
      "終了時刻が一番早いものを取れば、残った時間に他の区間を入れる余地が最大化される、"
      "という直感が証明の核心。"
  ),
  sections=[
      S("Step 1: 終了時刻でソート",
        "全区間を `(終了, 開始)` のペアでソート。終了時刻の昇順が基準。",
        diagram=(
            "flowchart LR\n"
            "  R[元の区間群] --> S[終了時刻昇順にソート]"
        )),
      S("Step 2: 左から貪欲に採用",
        "現在の「最後に採用した区間の終了時刻 `cur_end`」を持ち、"
        "次の区間の開始 ≥ `cur_end` なら採用。",
        diagram=(
            "flowchart TB\n"
            "  S[\"sorted intervals\"] --> L[for each]\n"
            "  L --> C{start >= cur_end?}\n"
            "  C -- Yes --> A[採用: cur_end = end, cnt += 1]\n"
            "  C -- No --> SK[スキップ]"
        )),
      S("Step 3: 正しさの証明",
        "「最適解 OPT に対し、終了が最も早い区間 g を採用して残りを処理する貪欲も同じ個数取れる」"
        "を交換論法で示す。直感的には **早く終わる = 残り時間に余裕** が増える。"),
  ],
  pitfalls=(
      "- 「重なる/重ならない」の境目: 端点が一致するときに重なるか? は問題による。\n"
      "- **開始時刻ソート** や **長さソート** にすると反例が作れて WA。\n"
      "- 「給料最大化」のような重み付きは別の DP (B39, C15) が必要。"
  ),
  related=["B39", "C15"])


L("A40",
  title="個数集計と組合せ数え上げ — 値ごとに数えて C(n, k) で合計",
  tagline="同じ値の組を数えるなら、値ごとに分けて組合せを足す",
  intro=(
      "「同じ値の組 (i, j, k) を数えろ」のような問題は、"
      "**値ごとに個数を集計し、それぞれで `C(cnt, k)` を足し合わせる** のが定石。\n\n"
      "二重ループで O(N²) や三重ループで O(N³) のところを、"
      "「値ごとに分類 → 各値について組合せ数を計算」で O(N + V) に圧縮できる。"
      "ここで V は値域 (例: 100 など)。"
  ),
  sections=[
      S("Step 1: 値ごとに個数を数える",
        "`cnt[v] = 値 v の出現回数`。`A` を 1 回パスして頻度配列を作る。",
        diagram=(
            "flowchart LR\n"
            "  A[\"A = [1,2,2,3,3,3]\"] --> C[\"cnt[1]=1, cnt[2]=2, cnt[3]=3\"]"
        )),
      S("Step 2: 各値について組合せ数を足す",
        "値 v が cnt 回出るなら、その値で k 個組を作る方法は `C(cnt, k)`。"
        "全ての値について足し合わせる。",
        diagram=(
            "flowchart LR\n"
            "  V1[\"v=1, cnt=1: C(1,3)=0\"] --> S[\"+\"]\n"
            "  V2[\"v=2, cnt=2: C(2,3)=0\"] --> S\n"
            "  V3[\"v=3, cnt=3: C(3,3)=1\"] --> S\n"
            "  S --> R[\"合計 = 1\"]"
        )),
      S("Step 3: C(n, k) の公式",
        "**`C(n, 3) = n × (n-1) × (n-2) / 6`** で計算できる (一般に `n × (n-1) × ... × (n-k+1) / k!`)。"
        "Python なら `math.comb(n, k)` で 1 行。"),
  ],
  pitfalls=(
      "- `C(cnt, k)` は `cnt < k` のとき 0。math.comb は自動で 0 を返す。\n"
      "- 「異なる値の組」を数えるときは、頻度配列だけでは足りず別法則 (B40) に切り替わる。\n"
      "- 値域が大きいときは頻度配列の代わりに `Counter` を使う。"
  ),
  related=["A30", "B40"])


L("A41",
  title="鳩ノ巣原理 — 「必ず同じ箱に 2 つ入る」",
  tagline="N+1 個を N 個の箱に入れたら、必ずどこかに 2 個入る",
  intro=(
      "**N+1 個の物を N 個の箱に入れたら、必ず 1 箱に 2 個以上入る**、という"
      "当たり前すぎる原理 (鳩ノ巣原理)。\n\n"
      "「N+1 文字あれば必ず同じ文字が 2 個ある」「N+1 個の数から差が K になる組がある」"
      "といった存在性の証明に使い、**「必ず存在する」ことを直接探索する** プログラムへつなげる。"
      "A41 では「3 連続同じ文字が必ず存在する」を鳩ノ巣で示し、その位置を線形探索で見つける。"
  ),
  sections=[
      S("Step 1: 箱と物の対応を考える",
        "「物 = N+1 個の何か」「箱 = N 個の分類」を作って、"
        "数を比較し「箱に 2 個入る」状況を作り出す。",
        diagram=(
            "flowchart LR\n"
            "  P[\"物 = N+1 個\"] --> B[\"箱 = N 個\"]\n"
            "  B --> R[\"必ず 1 箱に 2 個入る\"]"
        )),
      S("Step 2: 存在を線形探索で確認",
        "「必ず存在する」と数学的に分かったら、あとは線形探索で具体的に見つける。"
        "**「探せば必ず見つかる」と保証されているのがポイント**。"),
      S("Step 3: 応用 — 連続部分和 mod K",
        "鳩ノ巣の典型応用: 「N 個の累積和 mod K を見ると、同じ余りが必ずある (N > K)」 → "
        "その 2 つの差を取ると、和が K の倍数になる部分配列が見つかる。"),
  ],
  pitfalls=(
      "- **鳩ノ巣は存在性しか保証しない**。最適性や個数を求める問題には別法則。\n"
      "- 「箱の数 N、物の数 N+1」の関係を慎重に。「N+1 個」を確保できているか?\n"
      "- 「同じ文字が 3 連続」のような問題では、箱を「文字種類」、物を「3 個ずつ取った窓」と置く。"
  ),
  related=["B41"])


L("A42",
  title="2 軸を全探索 — (a, b) の組を全部試す",
  tagline="100 × 100 = 10^4 通りなら、毎回判定しても余裕",
  intro=(
      "「ある軸 (位置、しきい値、開始点) を全部試したいが、それぞれ 100 通り程度しかない」"
      "場合、**二重 for で全組を試す全探索** で十分速い。\n\n"
      "競プロでは「N が小さい」と書いてあったら全探索を真っ先に検討する。"
      "計算量 10^7〜10^8 まで Python で 1〜数秒。"
      "「全探索で WA が出ないか試してから、必要なら高速化」が安全な手順。"
  ),
  sections=[
      S("Step 1: 全探索の計算量を見積もる",
        "(a, b) を各 100 通り × 内側で N 回判定 = 10^6 × N。"
        "N ≤ 100 なら 10^8、Python で数秒程度で間に合う。",
        diagram=(
            "flowchart LR\n"
            "  A[\"a: 100 通り\"] --> P[\"×\"]\n"
            "  B[\"b: 100 通り\"] --> P\n"
            "  N[\"判定 N=100\"] --> P\n"
            "  P --> R[\"10^6 × 100 = 10^8\"]"
        )),
      S("Step 2: 二重 for で全組",
        "```python\n"
        "for a in range(1, 101):\n"
        "    for b in range(1, 101):\n"
        "        score = check(a, b)\n"
        "        answer = max(answer, score)\n"
        "```"),
      S("Step 3: 中の判定を関数化",
        "判定部分を別関数にすると、テストと修正が楽。"
        "全探索が WA なら、判定関数のロジックバグを疑う。"),
  ],
  pitfalls=(
      "- 計算量見積もりを最初に。Python は **10^7 操作で 1〜数秒** が目安。\n"
      "- 「正解の (a, b) が小数」「もっと範囲が広い」場合は二分探索 (A12) に切り替え。\n"
      "- 全探索でも工夫の余地はある (内側を貪欲・二分探索に置き換える A72 のパターン)。"
  ),
  related=["A03", "A05", "B42"])


L("A43",
  title="上界の最大 — 「各物体の最短距離」の max",
  tagline="「全体最小」を「個別最大」に分解",
  intro=(
      "「N 個の物体について、それぞれの最短距離 d_i の最大値を答えよ」のような問題。"
      "**個別の最短距離をまず求め、その max を取る** という単純な 2 段。\n\n"
      "個別の d_i の計算が O(M) ならば、全体 O(NM)。"
      "「最大値を最小化する」「最小値を最大化する」型の問題は、しばしば **答えで二分探索 (A12)** に書き換えられる。"
  ),
  sections=[
      S("Step 1: 各物体の最短距離を独立に計算",
        "BFS、ダイクストラ、距離公式など、問題に応じた方法で個別に計算。",
        diagram=(
            "flowchart LR\n"
            "  O[物体 1, 2, ..., N] --> D[各 d_i を計算]\n"
            "  D --> M[max d_i]"
        )),
      S("Step 2: 全体の答えは個別の max",
        "**`answer = max(d_1, d_2, ..., d_N)`**。\n\n"
        "「最も困っている個体」が全体の上限を決める、というイメージ。"),
      S("Step 3: 二分探索への一般化",
        "「max 距離 ≤ x 以下にできるか?」の判定問題に書き換えれば、二分探索 (A12) も使える。"
        "問題が大規模なときの選択肢。"),
  ],
  pitfalls=(
      "- 個別計算が遅いと全体も遅い。BFS / ダイクストラの選択を計算量で判断。\n"
      "- 「平均最短」「中央値」など他の集計関数を間違えない。\n"
      "- max を min に取り換えるパターン (A38) と紛らわしいが、構造は同じ。"
  ),
  related=["A38", "B43"])


L("A44",
  title="遅延処理 — 物理操作の代わりにフラグ管理",
  tagline="「実際に動かす」のはコストが大きい。代わりに方向や順序だけを記録",
  intro=(
      "「数列を反転する」「2 つの行を入れ替える」のような操作を毎回物理的に行うと"
      "O(N) かかります。代わりに **フラグや順序配列だけを更新** して、最後に出力時に解釈すると O(1)。\n\n"
      "代表的なテクニック:\n"
      "- 反転フラグ (A44 本問)\n"
      "- インデックス配列で行の順序を持つ (B44)\n"
      "- 遅延セグメント木 (上級)\n"
      "「物理的に動かすのは最後だけ」という発想。"
  ),
  sections=[
      S("Step 1: 状態を直接持たず、操作の記録だけ持つ",
        "反転操作なら **「反転されているか?」のブール 1 個** を持つ。"
        "操作 K 回 → ブール反転 K 回 → 出力時に解釈。",
        diagram=(
            "flowchart LR\n"
            "  A[反転操作 1] --> F[flag = True]\n"
            "  F --> B[反転操作 2]\n"
            "  B --> F2[flag = False]"
        )),
      S("Step 2: アクセス時にフラグを参照",
        "**「位置 i にアクセス」のとき、フラグに応じてインデックスを変換** する。"
        "`real_i = (N - 1 - i) if flag else i`。",
        diagram=(
            "flowchart LR\n"
            "  Q[\"query 位置 i\"] --> F{flag?}\n"
            "  F -- True --> R1[\"real_i = N-1-i\"]\n"
            "  F -- False --> R2[\"real_i = i\"]"
        )),
      S("Step 3: 行入れ替えの例",
        "B44 のように行入れ替えは **インデックス配列** `idx[1..N]` を持ち、"
        "行 a, b の入れ替えを `idx[a], idx[b] = idx[b], idx[a]` だけで実現。"
        "実際の行データはコピーしない → O(1)。"),
  ],
  pitfalls=(
      "- フラグ管理は **読み書きの両方** でフラグ参照を忘れずに。\n"
      "- 複数フラグが絡むと管理が破綻しがち。**状態の意味** をコメントに明記する。\n"
      "- 遅延処理は **最終出力時** にも正しく解釈する必要がある。"
  ),
  related=["B44"])


L("A45",
  title="不変量の発見 — 操作で保たれる量を見抜く",
  tagline="A36 の発展形。総和、mod、bit ごとの偶奇など",
  intro=(
      "「操作を繰り返してある状態にできるか?」の判定で、"
      "**操作で必ず保たれる量 (不変量)** を見つければ、最初と目標を比べるだけで答えが出る。\n\n"
      "偶奇 (A36) は特殊な不変量。"
      "他にも **総和、特定の mod、各 bit の偶奇、XOR 値、置換のパリティ** など、"
      "問題ごとに様々な不変量がある。**手で 1 ステップ試して観察する** のが最短ルート。"
  ),
  sections=[
      S("Step 1: 操作を 1 回試す",
        "「(a, b, c) のうち 2 つを +1 / -1 する」のような操作を、具体例で試す。"
        "結果を観察し、**何が変わって何が変わらないか** をリストアップ。",
        diagram=(
            "flowchart LR\n"
            "  S1[\"(2, 3, 5)\"] -->|操作| S2[\"(3, 4, 5)\"]\n"
            "  S2 -.差.- D[\"a+b 変化、c 不変、a+b+c +2\"]"
        )),
      S("Step 2: 不変量の候補をリスト化",
        "- 総和\n"
        "- 各要素の偶奇 (mod 2)\n"
        "- mod 3, mod 4, ...\n"
        "- 特定の関数の値 (例: XOR、product)\n"
        "操作を複数回試して、本当に不変かを確認。"),
      S("Step 3: 必要十分性の確認",
        "不変量が一致 = 必要条件しか言えていない場合がある。"
        "**「一致するなら本当に到達可能か」** を別途証明する必要がある (構成的に動かしてみる、など)。"),
  ],
  pitfalls=(
      "- 「不変量が一致 = Yes」とは限らない。**必要十分** を確認する。\n"
      "- 不変量は複数あることが多い。問題に効くのはどれか? を試行錯誤。\n"
      "- 不変量を発見できないときは、状態数が小さければ BFS で全探索する。"
  ),
  related=["A32", "A36", "B45"])


# =====================================================================
#  Chapter 7 — ヒューリスティック (A46〜A50)
# =====================================================================

L("A46",
  title="貪欲法 (Nearest Neighbor) — その場で最良を取る",
  tagline="ヒューリスティック問題の最初の一歩",
  intro=(
      "TSP のような「巡回経路を求めて最短に」というヒューリスティック問題で、"
      "**現在地から最も近い未訪問都市へ移動** する単純な貪欲。\n\n"
      "最適解は保証しないが、**実装が単純で初期解として優秀**。"
      "山登り (A47) や焼きなまし (A48) の出発点として使う。"
      "競プロのマラソンマッチでも「まず貪欲」が定石。"
  ),
  sections=[
      S("Step 1: 初期都市を決める",
        "都市 0 から出発と決める。`visited[0] = True`、経路 P = [0]。",
        diagram=(
            "flowchart LR\n"
            "  S[都市 0] --> N1[次に行ける都市 1..N-1]"
        )),
      S("Step 2: 最近傍を選ぶ",
        "未訪問の中で **現在地から最も近い都市** を全探索 O(N) で見つける。"
        "選んだら経路に追加、訪問済みにする。",
        diagram=(
            "flowchart TB\n"
            "  C[現在地] --> S[未訪問都市を全部見る]\n"
            "  S --> M[最小距離を探す]\n"
            "  M --> A[\"P.append(min_id)\"]\n"
            "  A --> V[\"visited[min_id] = True\"]"
        )),
      S("Step 3: 最後にスタートへ戻る",
        "N 回繰り返したあと、`P.append(0)` でループを閉じる。"
        "経路長は `Σ dist(P[i], P[i+1])`。"),
  ],
  pitfalls=(
      "- 貪欲は **最適性なし**。「だいたい良い」答えしか出ない。\n"
      "- 同距離の都市が複数あるときの選び方で結果がぶれる。\n"
      "- 局所最適に陥りやすい。改善には山登り (A47) や焼きなまし (A48)。"
  ),
  related=["A47", "A48", "A49"])


L("A47",
  title="山登り法 — 改善する変更だけ受理",
  tagline="ランダムに動かして、良くなったら採用",
  intro=(
      "**ランダムに小さい変更を加え、スコアが改善した場合のみ採用** する局所探索。"
      "「2-opt 反転」のような近傍操作と組み合わせて使う。\n\n"
      "実装は短いが、**局所最適から抜け出せない弱点** がある。"
      "焼きなまし (A48) はこの弱点を確率で「あえて悪化を受理」して回避する。"
  ),
  sections=[
      S("Step 1: 初期解と現在スコア",
        "貪欲 (A46) で初期解を作るか、ランダム順列から始める。"
        "現在の経路長を `current_score` として持つ。"),
      S("Step 2: 近傍操作 (2-opt)",
        "ランダムな区間 `[l, r]` を選んで反転する。"
        "TSP では「2 本の辺を 2 本の別の辺に張り替える」操作に対応。",
        diagram=(
            "flowchart LR\n"
            "  P1[\"P = ...A,B,...,C,D,...\"] -->|区間 B..C 反転| P2[\"P' = ...A,C,...,B,D,...\"]"
        )),
      S("Step 3: 改善判定",
        "反転後のスコアを計算。改善なら採用、悪化なら反転を取り消す。\n\n"
        "```python\n"
        "if new_score < current_score:\n"
        "    current_score = new_score  # 採用\n"
        "else:\n"
        "    P[l:r+1] = reversed(P[l:r+1])  # 取り消し\n"
        "```"),
  ],
  pitfalls=(
      "- 反転スコア計算は 2 辺の差分だけで O(1) に高速化できる (TSP の差分計算)。\n"
      "- 局所最適に必ず陥る。脱出には焼きなまし (A48) や乱数再起動 (Restart)。\n"
      "- 乱数の seed を固定するとデバッグが楽。"
  ),
  related=["A46", "A48"])


L("A48",
  title="焼きなまし法 — 確率で悪化も受理する",
  tagline="温度に応じて、悪い変更も少し受け入れる",
  intro=(
      "山登り法 (A47) の欠点 (局所最適への陥り) を **「確率的に悪化を受理する」** ことで回避するヒューリスティック。\n\n"
      "受理確率 **`exp((new_score - cur_score) / T)`** (最大化の場合)。"
      "**温度 T を時間とともに下げていく** ことで、序盤は広く探索 → 終盤は厳しく改善のみ受理、"
      "という挙動を作る。マラソンマッチの定番。"
  ),
  sections=[
      S("Step 1: 山登りと同じ近傍操作",
        "ランダムに 2-opt 反転などを試す。これは山登りと同じ。"),
      S("Step 2: 受理判定を確率に",
        "改善なら必ず採用。悪化なら **確率 `exp(Δ / T)`** で採用 (Δ は変化量、最大化なら正が改善)。\n\n"
        "T が大きい (序盤) → 多少の悪化も受理 → 広く探索\n"
        "T が小さい (終盤) → 悪化を拒否 → 山登りに近づく",
        diagram=(
            "flowchart LR\n"
            "  D{Δ > 0?}\n"
            "  D -- Yes --> A[必ず受理]\n"
            "  D -- No --> P[\"確率 exp(Δ/T) で受理\"]"
        )),
      S("Step 3: 温度スケジュール",
        "時間 t に対し、`T = T_start × (T_end / T_start)^(t / T_max)` のように **指数的に冷却**。"
        "`T_start = 大きめ`、`T_end = 小さめ` を経験的に決める。"),
  ],
  pitfalls=(
      "- 温度のチューニングは経験頼り。テストデータで何度か試して調整。\n"
      "- `exp` は浮動小数のオーバーフローに注意。事前に Δ をクランプ。\n"
      "- 制限時間ギリギリまでループを回す (例: `while time.time() - start < 0.95`)。"
  ),
  related=["A47", "A50"])


L("A49",
  title="ビームサーチ — 状態を上位 W 件に絞って前進",
  tagline="毎ターン上位 W 件だけ残し、その全候補を次に展開",
  intro=(
      "**全状態を持つと爆発するが、上位 W 件 (= ビーム幅) だけ残せば現実的**、"
      "という探索法。\n\n"
      "1 手前進するごとに、現在の W 個の状態それぞれから可能な遷移を全列挙、"
      "全候補をスコア順にソートして上位 W 個だけ次のステップに残す。"
      "**「貪欲ではちょっと足りない、全探索は無理」** な中間で活躍。"
  ),
  sections=[
      S("Step 1: 初期状態 1 個",
        "ビーム `beam[0] = [初期状態]`。手番 i = 0..T-1 で 1 個ずつ進む。",
        diagram=(
            "flowchart LR\n"
            "  B0[1 個] --> B1[W 個まで膨張]\n"
            "  B1 --> B2[W 個]\n"
            "  B2 --> B3[W 個]"
        )),
      S("Step 2: 全候補を展開",
        "現在の beam の各状態から **可能な遷移を全部** 試し、新しい状態の候補を集める。",
        diagram=(
            "flowchart TB\n"
            "  S1[状態 1] --> N1A[遷移 A]\n"
            "  S1 --> N1B[遷移 B]\n"
            "  S2[状態 2] --> N2A[遷移 A]\n"
            "  S2 --> N2B[遷移 B]\n"
            "  N1A & N1B & N2A & N2B --> C[候補プール]"
        )),
      S("Step 3: 上位 W 件に絞る",
        "候補プールをスコア順にソート、上位 W 件だけ `beam[i+1]` に残す。"
        "**W を大きくすると質は上がるが計算量も増える** (W = 100〜10000 が目安)。"),
  ],
  pitfalls=(
      "- 状態のコピーが重い (deepcopy)。差分更新で高速化することが多い。\n"
      "- 状態のハッシュで重複除去すると効果絶大。\n"
      "- 復元用に「前の手番のどこから来たか」を各状態に持たせる。"
  ),
  related=["A48", "A50"])


L("A50",
  title="差分計算 — 全部計算し直さず差だけ更新",
  tagline="変更したのは一部だけ。だから差だけ再計算",
  intro=(
      "ヒューリスティックの内側ループで、**毎回スコア全体を計算するのは無駄**。"
      "状態を 1 箇所だけ変えたなら、**そこの差分だけを足し引きすれば** 全体スコアが O(1) で更新できる。\n\n"
      "TSP の 2-opt なら、「外した 2 辺と、新しい 2 辺」の差を計算するだけで O(1)。"
      "ループ回数を 100 倍に増やせる本気の高速化テクニック。"
  ),
  sections=[
      S("Step 1: スコアを構造的に分解",
        "「全体スコア = Σ 個別寄与」の形に書き直す。"
        "TSP なら 各辺の長さの和、最大カット問題なら各辺の貢献度の和、など。",
        diagram=(
            "flowchart LR\n"
            "  TS[全体スコア] --> S[Σ 個別寄与]"
        )),
      S("Step 2: 変更箇所の差分だけ計算",
        "変更前の寄与を引いて、変更後の寄与を足す。"
        "TSP 2-opt なら **2 辺の差** で O(1)。",
        diagram=(
            "flowchart LR\n"
            "  C[\"current_score\"] --> M[\"- 旧辺コスト\"]\n"
            "  M --> P[\"+ 新辺コスト\"]\n"
            "  P --> N[\"new_score\"]"
        )),
      S("Step 3: numpy などで更に高速化",
        "Python のオーバーヘッドが効くときは **numpy** で配列演算化する手も。"
        "C 言語並みの速度が出ることがある。"),
  ],
  pitfalls=(
      "- 差分計算で **小さなバグ** が入ると最終答えがズレる。"
      "  たまに全部再計算してチェックする手もアリ。\n"
      "- 浮動小数の累積誤差。1000 ループに 1 回くらい全部再計算でリセット。\n"
      "- 状態のコピーを deepcopy で行うとそれ自体が遅い。**シャロー化** や **元に戻す方式** を検討。"
  ),
  related=["A47", "A48"])


# =====================================================================
#  Chapter 8 — データ構造とクエリ処理 (A51〜A60)
# =====================================================================

L("A51",
  title="スタック — 後入れ先出し (LIFO)",
  tagline="最後に入れたものを最初に取り出す",
  intro=(
      "**後入れ先出し (LIFO)** のデータ構造。"
      "「最後に入れたもの」を最初に取り出すという挙動。"
      "Python では `list` の `append` / `pop` か、`collections.deque` の `append` / `pop` で実現。\n\n"
      "**関数の呼び出し履歴、DFS、括弧マッチング、Histogram の最大長方形** など、"
      "「後で戻ってきて処理する」性質の問題で頻出。"
  ),
  sections=[
      S("Step 1: 操作",
        "- **push**: 末尾に追加 (`stack.append(x)`)\n"
        "- **pop**: 末尾を取り出す (`stack.pop()`)\n"
        "- **top**: 末尾を見る (`stack[-1]`)\n"
        "全て O(1)。",
        diagram=(
            "flowchart LR\n"
            "  E[空] -->|push 1| S1[1]\n"
            "  S1 -->|push 2| S2[\"1, 2\"]\n"
            "  S2 -->|push 3| S3[\"1, 2, 3\"]\n"
            "  S3 -->|pop = 3| S4[\"1, 2\"]"
        )),
      S("Step 2: list vs deque",
        "Python の `list` も末尾操作は O(1) なのでスタックとして使える。"
        "`deque` の方がメモリ効率が良いが、競プロでは `list` で十分。"),
      S("Step 3: DFS のスタック実装",
        "再帰 DFS を **明示的なスタック** で書けば、再帰上限を気にせず深い探索ができる。\n\n"
        "```python\n"
        "stack = [start]\n"
        "while stack:\n"
        "    v = stack.pop()\n"
        "    for u in G[v]:\n"
        "        if not visited[u]:\n"
        "            visited[u] = True\n"
        "            stack.append(u)\n"
        "```"),
  ],
  pitfalls=(
      "- 空のスタックで `pop()` は IndexError。`if stack:` でガード。\n"
      "- `deque` の場合は `append` / `pop`、`popleft` は使わない (使うとキューになる)。\n"
      "- 再帰 DFS のスタック上限は Python 1000。`sys.setrecursionlimit(10**6)` で拡張可。"
  ),
  related=["A60", "A62", "B51"])


L("A52",
  title="キュー — 先入れ先出し (FIFO)",
  tagline="入った順に取り出す。BFS の中核",
  intro=(
      "**先入れ先出し (FIFO)** のデータ構造。"
      "最初に入れたものを最初に取り出す。"
      "Python では `collections.deque` を使う (list は `popleft` が O(N) で遅い)。\n\n"
      "**BFS、シミュレーション、生産消費** などで使う。"
      "両端から O(1) で操作できる `deque` は、スタック/キューの両方の役割をこなせる優秀なデータ構造。"
  ),
  sections=[
      S("Step 1: 操作",
        "- **enqueue**: 末尾に追加 (`q.append(x)`)\n"
        "- **dequeue**: 先頭を取り出す (`q.popleft()`)\n"
        "- **front**: 先頭を見る (`q[0]`)\n"
        "全て O(1)。",
        diagram=(
            "flowchart LR\n"
            "  E[空] -->|append 1| Q1[1]\n"
            "  Q1 -->|append 2| Q2[\"1, 2\"]\n"
            "  Q2 -->|append 3| Q3[\"1, 2, 3\"]\n"
            "  Q3 -->|popleft = 1| Q4[\"2, 3\"]"
        )),
      S("Step 2: BFS の典型実装",
        "```python\n"
        "from collections import deque\n"
        "q = deque([start])\n"
        "dist[start] = 0\n"
        "while q:\n"
        "    v = q.popleft()\n"
        "    for u in G[v]:\n"
        "        if dist[u] == -1:\n"
        "            dist[u] = dist[v] + 1\n"
        "            q.append(u)\n"
        "```"),
      S("Step 3: deque は両端 O(1)",
        "`append`, `pop`, `appendleft`, `popleft` 全て O(1)。"
        "両端から取り出せる **デック (double-ended queue)** として、"
        "0-1 BFS や C17 のような特殊用途にも便利。"),
  ],
  pitfalls=(
      "- `list.pop(0)` は O(N) で遅い。**必ず `deque.popleft()`** を使う。\n"
      "- 空のときの `popleft()` は IndexError。\n"
      "- `queue` モジュール (Queue 標準ライブラリ) はスレッド安全な代わりに遅い。競プロでは deque。"
  ),
  related=["A51", "A63", "C17"])


L("A53",
  title="優先度付きキュー — 最小値 / 最大値を高速に取り出す",
  tagline="ヒープ。挿入と最小値削除が O(log N)",
  intro=(
      "**最小値 (または最大値) を効率的に取り出す** ことに特化したデータ構造。"
      "二分ヒープが標準的な実装で、挿入と取り出しが共に O(log N)。\n\n"
      "Python は `heapq` モジュール。**最小ヒープ専用** なので、"
      "最大値が欲しいときは **−1 倍して入れる** トリックを使う。"
      "ダイクストラ (A64)、ジョブスケジューリング (B53) などで必須。"
  ),
  sections=[
      S("Step 1: 操作",
        "- **push**: `heappush(h, x)` — O(log N)\n"
        "- **pop min**: `heappop(h)` — O(log N)\n"
        "- **peek min**: `h[0]` — O(1)",
        diagram=(
            "flowchart LR\n"
            "  P[heappush] --> H[ヒープ内部で並べ替え]\n"
            "  H --> R[\"h[0] が常に最小\"]"
        )),
      S("Step 2: 最大ヒープへの変換",
        "Python は最小ヒープしかないので、**−1 倍** して入れる。\n\n"
        "```python\n"
        "heapq.heappush(h, -x)\n"
        "max_x = -heapq.heappop(h)\n"
        "```"),
      S("Step 3: タプルで複合キー",
        "`(priority, payload)` のタプルを入れると、優先度順にソートされる。"
        "ダイクストラの `(距離, 頂点)` ペアが典型例。"),
  ],
  pitfalls=(
      "- `heapq` は **最小ヒープのみ**。最大が必要なら `-x` で入れる。\n"
      "- `h[1]` は 2 番目に小さい値ではない (ヒープ構造のため)。\n"
      "- 既存のリストを `heapify(h)` でヒープ化すると O(N) (一個ずつ push より速い)。"
  ),
  related=["A64", "B53"])


L("A54",
  title="連想配列 — 辞書による key-value 管理",
  tagline="ハッシュで平均 O(1) アクセス",
  intro=(
      "**「名前 → 値」のような任意のキーで O(1) アクセスできる** データ構造。"
      "Python では `dict` がそれ。内部はハッシュテーブルで、平均 O(1) で挿入・参照。\n\n"
      "出現回数のカウント (`collections.Counter`)、グループごとの集計、メモ化など、"
      "競プロで最も多用するデータ構造の 1 つ。"
  ),
  sections=[
      S("Step 1: 基本操作",
        "- 挿入/更新: `d[key] = value`\n"
        "- 取得: `d[key]` (存在しないと KeyError)\n"
        "- 安全に取得: `d.get(key, default)`\n"
        "全て平均 O(1)。",
        diagram=(
            "flowchart LR\n"
            "  K[key] --> H[hash]\n"
            "  H --> S[内部スロット]\n"
            "  S --> V[value]"
        )),
      S("Step 2: Counter で頻度集計",
        "```python\n"
        "from collections import Counter\n"
        "c = Counter(A)\n"
        "# c['x'] = 'x' の出現回数 (なければ 0)\n"
        "```\n"
        "競プロで最頻出のクラス。`most_common(k)` で上位 k 件を取れる。"),
      S("Step 3: defaultdict で初期値自動",
        "`defaultdict(int)` は新しいキーへのアクセス時に自動で 0 を返す。"
        "「キーが存在しないかもしれない」のチェックが省略でき、コードが短くなる。"),
  ],
  pitfalls=(
      "- 同じキーを 2 回 push すると、値は上書きされる。`Counter` なら自動で加算してくれる。\n"
      "- ハッシュ攻撃を意識する競技は稀。素直に `dict` を使って OK。\n"
      "- イミュータブル (tuple, str) しかキーにできない。list はキーにできない。"
  ),
  related=["A55", "B54"])


L("A55",
  title="順序付き集合 — 値の挿入/削除と「以上/以下」検索",
  tagline="C++ の std::set 相当。Python では SortedSet ライブラリ",
  intro=(
      "「値の挿入、削除、x 以上の最小要素、x 以下の最大要素」を **全て O(log N)** で行えるデータ構造。"
      "**C++ の `std::set`** が標準的だが、Python の標準ライブラリには無いので外部ライブラリ (SortedSet) を使う。\n\n"
      "「最も近い値」「順位」「上位 K 個」などのクエリで威力を発揮。"
      "難問の構築テクとして頻出する一方、初学者には実装の選択肢が乏しいのが Python の悩み。"
  ),
  sections=[
      S("Step 1: できること",
        "- 挿入: `s.add(x)` / 削除: `s.discard(x)` — O(√N) or O(log N)\n"
        "- x 以上の最小: `s.ge(x)` — O(√N)\n"
        "- x 以下の最大: `s.le(x)` — O(√N)\n"
        "- 順位: `s.index(x)`",
        diagram=(
            "flowchart LR\n"
            "  Q[クエリ: x] --> G[ge x: 以上の最小]\n"
            "  Q --> L[le x: 以下の最大]\n"
            "  Q --> D[\"差 = min(x-le, ge-x)\"]"
        )),
      S("Step 2: Python での選択肢",
        "- **SortedSet ライブラリ** (tatyam-prime/SortedSet): 平方分割ベース、O(√N)\n"
        "- **sortedcontainers.SortedSet**: B 木ベース、AtCoder にプリインストール\n"
        "- 自作 BIT で番兵 (高度)\n\n"
        "本サイトでは SortedSet ライブラリを採用 (B55)。"),
      S("Step 3: 番兵テクニック",
        "「x より小さい数がない」「x より大きい数がない」のケース回避に、"
        "**`-INF` と `+INF` を最初から入れておく** と分岐が消える。"),
  ],
  pitfalls=(
      "- Python 標準にはない。AtCoder では `sortedcontainers` が使える。\n"
      "- 重複を扱いたいなら **SortedList** (set ではなく list の順序版) を使う。\n"
      "- 番兵を入れたら、「実要素ゼロ」の判定を `len(s) == 2` で行う。"
  ),
  related=["A11", "B55"])


L("A56",
  title="ローリングハッシュ — 部分文字列を 1 個の数値に",
  tagline="連続部分列の一致判定を O(1) に",
  intro=(
      "文字列 S の **連続部分列を 1 つの整数 (ハッシュ値) に変換** し、"
      "前計算しておくことで、部分列の比較を **O(1)** で行える。\n\n"
      "**`H[i] = S[0..i-1] のハッシュ`** を多項式表現で前計算しておけば、"
      "部分列 S[l..r] のハッシュは `H[r] - H[l-1] × B^(r-l+1)` で取れる。"
      "**衝突確率 1/M** なので、素数 M を選んで十分大きく取る。"
  ),
  sections=[
      S("Step 1: 多項式ハッシュ",
        "`H[i] = S[0]·B^(i-1) + S[1]·B^(i-2) + ... + S[i-1]·B^0  (mod M)`。"
        "ベースは 100 や 31、mod は 10^9+7 や 2^61-1 が定番。",
        diagram=(
            "flowchart LR\n"
            "  S[文字 → 数値] --> P[\"基数 B のべき乗で重みづけ\"]\n"
            "  P --> A[\"+ で累積\"]\n"
            "  A --> H[\"H[i]\"]"
        )),
      S("Step 2: 部分列のハッシュ",
        "`H[r] - H[l-1] × B^(r - l + 1)` で取れる。"
        "**長い接頭辞から短い接頭辞の貢献を引く** という発想で、"
        "1 次元累積和 (A06) の積版と思うとよい。"),
      S("Step 3: 衝突対策",
        "**異なる文字列が同じハッシュ値** になる確率は約 1/M。"
        "競プロでは **mod を 2 つ用意して両方一致** を確認する流儀 (双 mod) で安全性を高める。"),
  ],
  pitfalls=(
      "- mod は **素数** にする。10^9+7 / 998244353 / 2^61-1 が定番。\n"
      "- ベースは 1 より大きい整数。1 では情報が消える。\n"
      "- 双 mod でも完全衝突なしは保証されない。最後の手段は実比較。"
  ),
  related=["A06", "B56"])


L("A57",
  title="ダブリング — 2^d 先の遷移を前計算",
  tagline="K 回後の状態を O(log K) で",
  intro=(
      "「`A[i]` が i の遷移先」と定義したとき、"
      "**K 回後の到達先** を素朴に追うと O(K)。"
      "代わりに **`dp[d][i] = i から 2^d 回後の位置`** を前計算しておけば、"
      "K を 2 進数で分解して O(log K) で答えられる。\n\n"
      "K = 10^18 でも 60 ステップで済む。"
      "**LCA (最小共通祖先)** や **状態遷移の K 回後** など、汎用性は高い。"
  ),
  sections=[
      S("Step 1: テーブルの定義",
        "**`dp[d][i] = 位置 i から 2^d 回後の到達先`**。"
        "`dp[0][i] = A[i]` (= 1 回後)。",
        diagram=(
            "flowchart LR\n"
            "  D0[\"dp[0][i] = A[i]\"] -->|二乗| D1[\"dp[1][i] = dp[0][dp[0][i]]\"]\n"
            "  D1 -->|二乗| D2[\"dp[2][i] = dp[1][dp[1][i]]\"]"
        )),
      S("Step 2: 前計算",
        "`dp[d][i] = dp[d-1][dp[d-1][i]]`。"
        "`d` を 1 から LEVELS-1 まで埋める。LEVELS は ceil(log2(K_max))。"),
      S("Step 3: K 回後のクエリ",
        "K を 2 進数で分解し、bit が立った位の `dp[d][i]` で遷移していく。\n\n"
        "```python\n"
        "for d in range(LEVELS):\n"
        "    if (K >> d) & 1:\n"
        "        i = dp[d][i]\n"
        "```"),
  ],
  pitfalls=(
      "- LEVELS の取り方。`2^60 > 10^18` なので 60 で十分。\n"
      "- `A[i]` が 1-indexed か 0-indexed かで前処理が変わる。"
      "  `A[i] - 1` などで揃える。\n"
      "- 「最小公倍数」や「累積和」もダブリングで取れる (LCA 等)。"
  ),
  related=["A29", "B57"])


L("A58",
  title="セグメント木 (RMQ) — 区間最大値を O(log N) で",
  tagline="配列を木に変換。任意区間の集約と 1 点更新が log N",
  intro=(
      "配列の **任意区間の最大値** を O(log N) で求め、**1 点更新** も O(log N) で行えるデータ構造。"
      "「完全二分木で、葉が元の配列、親が子 2 つの max」という単純な構造。\n\n"
      "max を sum に取り換えれば RSQ (A59)、min / GCD / XOR など、"
      "**結合則 + 単位元** を持つ任意の演算で同じ枠組みが使える。"
      "「区間更新 + 区間クエリ」が欲しい場合は **遅延セグメント木** に拡張。"
  ),
  sections=[
      S("Step 1: 木の構造",
        "N 以上の最小の 2 のべき乗を `size` とし、"
        "**配列サイズ 2·size** の配列で完全二分木を表現。"
        "葉は `size..2·size-1`、親は子 2 つの max。",
        diagram=(
            "flowchart TB\n"
            "  R[root] --> L1[\"[0,4)\"]\n"
            "  R --> R1[\"[4,8)\"]\n"
            "  L1 --> L2[\"[0,2)\"]\n"
            "  L1 --> L3[\"[2,4)\"]\n"
            "  L2 --> A0[A0]\n"
            "  L2 --> A1[A1]"
        )),
      S("Step 2: 1 点更新",
        "葉を書き換え、**親方向へ max を再計算**。深さは log(size) なので O(log N)。",
        diagram=(
            "flowchart TB\n"
            "  L[葉を更新] --> P[親 = max 子 2 つ]\n"
            "  P --> P2[祖父 = max 子 2 つ]\n"
            "  P2 --> R[root へ]"
        )),
      S("Step 3: 区間クエリ",
        "半開区間 `[l, r)` を再帰的に分解:\n"
        "- 重ならない → 単位元 (max なら `-∞`) を返す\n"
        "- 完全に含まれる → そのノードの値を返す\n"
        "- 部分的に重なる → 左右の子に分けて max"),
  ],
  pitfalls=(
      "- 単位元の選択を間違えるとバグる。max → `-∞`、sum → `0`、min → `+∞`、GCD → `0`。\n"
      "- 半開区間 `[l, r)` で書くと境界処理が一貫する。\n"
      "- 区間更新 + 1 点取得には **遅延セグ木**。さらに区間更新 + 区間取得は遅延伝播も。"
  ),
  related=["A59", "A60", "C19"])


L("A59",
  title="セグメント木 (RSQ) — 区間和に演算を取り替える",
  tagline="A58 と同じ構造。max を sum に変えるだけ",
  intro=(
      "A58 のセグメント木の **演算を max → sum に変えただけ** で区間和クエリ (RSQ) になる。\n\n"
      "より一般に、**結合則を持ち単位元がある任意の演算** (sum, max, min, GCD, XOR, 行列積…) で"
      "同じ枠組みが使える。「Segment Tree は 1 つ覚えれば 10 個の問題が解ける」と言われる所以。"
  ),
  sections=[
      S("Step 1: 単位元と演算の変更",
        "- sum: 単位元 0、演算 +\n"
        "- max: 単位元 -∞、演算 max\n"
        "- min: 単位元 +∞、演算 min\n"
        "- GCD: 単位元 0、演算 gcd\n"
        "- XOR: 単位元 0、演算 ^",
        diagram=(
            "flowchart LR\n"
            "  S[セグ木テンプレ] --> M[演算と単位元を差し替え]\n"
            "  M --> R[max/sum/min/GCD/XOR…]"
        )),
      S("Step 2: 転倒数への応用 (B59)",
        "**「自分より大きい要素の個数」を BIT / セグ木で逐次集計** すれば、"
        "全体 O(N log N) で転倒数が求まる。"
        "**座標圧縮 (A15)** との合わせ技が定番。"),
      S("Step 3: 区間最小値最短ジャンプ (B58)",
        "DP + セグ木の合わせ技。"
        "`dp[i] = 1 + min(dp[i - R], ..., dp[i - L])` のような遷移を"
        "セグ木の RMQ で取ると O(N log N)。"),
  ],
  pitfalls=(
      "- 単位元を演算の **左右で同じ** にする (i.e., 可換でなくても結合則は必要)。\n"
      "- 区間和なら `int` のオーバーフローを (Python は OK だが C/C++ は注意)。\n"
      "- 平方分割や Fenwick (BIT) でも代用できることがある。問題の制約に応じて選択。"
  ),
  related=["A58", "B58", "B59"])


L("A60",
  title="スタックの活用 — 「自分以上の最後の日」を線形時間で",
  tagline="ヒストグラム / 株価系の典型。Monotonic Stack",
  intro=(
      "「各日について、自分の値 **以上** の値が最後に現れる日」のような問題を、"
      "**単調スタック (Monotonic Stack)** で全体 O(N) で解く技法。\n\n"
      "スタックには「まだ答えが確定していない日」が入っている。"
      "新しい値が来たとき、スタック頭の値より大きければ、その日の答えが今確定する → pop。"
      "「ヒストグラムの最大長方形」「気温が次に上がる日」などの古典問題で使う。"
  ),
  sections=[
      S("Step 1: スタックの中身を定義",
        "**「まだ自分の答え (= 自分以上の値が次に現れる日) が確定していない日」** をスタックに積む。"
        "スタックは **下から上へ値が小さくなる** ように保つ (= 単調)。"),
      S("Step 2: 新しい日が来たら pop",
        "新しい日の値 v が来たとき、スタックの top の値 ≤ v なら、"
        "**スタックの top の日に対する答えは新しい日**。pop して回答を確定。"
        "これを top の値 > v になるまで繰り返す。",
        diagram=(
            "flowchart TB\n"
            "  N[新しい日 v] --> W[while stack top の値 ≤ v]\n"
            "  W --> P[pop して回答確定]\n"
            "  W --> A[新しい日を push]"
        )),
      S("Step 3: 全体 O(N)",
        "各日は **多くても 1 回 push、1 回 pop** されるだけなので、"
        "全体の操作数は 2N で O(N)。各日ごとに二分探索や線形探索より圧倒的に速い。"),
  ],
  pitfalls=(
      "- 「以上」と「より大」の境目で **等号** の扱いを間違えると 1 ずれる。\n"
      "- 単調性の方向 (増加 / 減少) は問題による。\n"
      "- 残ったスタックの要素は「答えなし」(右に大きい値がない)。-1 などのセンチネルで処理。"
  ),
  related=["A51"])


# =====================================================================
#  Chapter 9 — グラフアルゴリズム (A61〜A70)
# =====================================================================

L("A61",
  title="グラフの表現 — 隣接リストが基本",
  tagline="頂点ごとに「繋がっている頂点のリスト」を持つ",
  intro=(
      "グラフをプログラムで扱う方法は **隣接行列** と **隣接リスト** の 2 通り。"
      "競プロでは **隣接リストが標準**。空間 O(V+E)、辺の走査が O(degree)。\n\n"
      "**`G[v] = [v に繋がっている頂点のリスト]`** で表す。"
      "無向グラフは双方向に登録、有向は片方向のみ。"
      "重み付きなら `(隣接頂点, 重み)` のタプルを入れる。"
  ),
  sections=[
      S("Step 1: 隣接リスト",
        "```python\n"
        "G = [[] for _ in range(N + 1)]\n"
        "for _ in range(M):\n"
        "    a, b = map(int, input().split())\n"
        "    G[a].append(b)\n"
        "    G[b].append(a)  # 無向なら両方\n"
        "```",
        diagram=(
            "flowchart LR\n"
            "  G1[\"G[1] = [2, 3]\"] -.- N1[\"1 → 2, 1 → 3\"]\n"
            "  G2[\"G[2] = [1, 3, 4]\"] -.- N2[\"2 → 1, 3, 4\"]"
        )),
      S("Step 2: 隣接行列との比較",
        "**隣接行列** `M[i][j] = 1 if edge else 0` は密グラフ向き、空間 O(V²)。"
        "**隣接リスト** は疎グラフ向き、空間 O(V+E)。"
        "競プロは疎が多いのでリストが多数派。",
        diagram=(
            "flowchart LR\n"
            "  L[隣接リスト<br/>空間 O V+E] --> S[疎グラフに最適]\n"
            "  M[隣接行列<br/>空間 O V^2] --> D[密グラフに最適]"
        )),
      S("Step 3: 重み付きグラフ",
        "**`G[v].append((u, w))`** でタプルにする。"
        "ダイクストラ (A64) や MST (A67) はこの形式が必須。"),
  ],
  pitfalls=(
      "- 1-indexed の問題が多い。`G = [[] for _ in range(N + 1)]` で 1 個余分に取る。\n"
      "- 無向グラフで `G[b].append(a)` を忘れると一方通行に。\n"
      "- 多重辺・自己ループの扱いは問題による。問題文を確認。"
  ),
  related=["A62", "A63", "A64", "A66", "A67"])


L("A62",
  title="DFS — 深さ優先探索で連結判定",
  tagline="行けるだけ進んで、戻ってきたら別の道",
  intro=(
      "頂点 v から **「行けるだけ進む」** 探索。"
      "再帰またはスタックで実装でき、計算量は O(V+E)。\n\n"
      "用途: 連結判定、橋・関節点検出、サイクル検出、トポロジカルソート、木 DP の前処理 など。"
      "再帰版は短く書けるが、**Python は再帰上限 1000** がデフォなので大きいグラフでは要拡張。"
  ),
  sections=[
      S("Step 1: 再帰版",
        "```python\n"
        "sys.setrecursionlimit(10**6)\n"
        "def dfs(v):\n"
        "    visited[v] = True\n"
        "    for u in G[v]:\n"
        "        if not visited[u]:\n"
        "            dfs(u)\n"
        "```",
        diagram=(
            "flowchart TB\n"
            "  V[頂点 v] --> M[visited v = True]\n"
            "  M --> L[全隣接 u を確認]\n"
            "  L --> R{visited u?}\n"
            "  R -- No --> D[dfs u 再帰]\n"
            "  R -- Yes --> L"
        )),
      S("Step 2: スタック版 (非再帰)",
        "再帰上限を気にせずに済む。深いグラフでは必須。\n\n"
        "```python\n"
        "stack = [start]\n"
        "visited[start] = True\n"
        "while stack:\n"
        "    v = stack.pop()\n"
        "    for u in G[v]:\n"
        "        if not visited[u]:\n"
        "            visited[u] = True\n"
        "            stack.append(u)\n"
        "```"),
      S("Step 3: 連結判定",
        "DFS 後、`all(visited[1..N])` なら連結。\n\n"
        "用途を広げると、**連結成分の個数** = DFS を始められた回数。"),
  ],
  pitfalls=(
      "- Python の再帰上限 1000。`sys.setrecursionlimit(10**6)` で拡張。\n"
      "- グリッド系は (i, j) を 1 頂点として展開。隣接は上下左右 4 方向。\n"
      "- visited 配列のサイズは V+1 (1-indexed の場合)。"
  ),
  related=["A51", "A61", "A63", "B62"])


L("A63",
  title="BFS — 幅優先探索で最短歩数",
  tagline="近い順に層を広げる。重みなし最短路の基本",
  intro=(
      "頂点 s から **距離が近い順** に層状に広げる探索。"
      "キュー (deque) を使い、計算量 O(V+E)。\n\n"
      "**重みなしグラフ (またはすべての辺の重みが等しいグラフ) の最短路** を求めるのに使う。"
      "迷路、迷宮、グリッド最短路の定番。"
      "重み付きが必要ならダイクストラ (A64) に切り替え。"
  ),
  sections=[
      S("Step 1: 初期化",
        "`dist = [-1] * (N+1)`、`dist[s] = 0`、`q = deque([s])`。",
        diagram=(
            "flowchart LR\n"
            "  S[start s] --> D[\"dist[s] = 0\"]\n"
            "  D --> Q[\"q = [s]\"]"
        )),
      S("Step 2: 層を広げる",
        "キューの先頭を取り出し、隣接頂点の `dist` をまだ未訪問なら +1 して push。",
        diagram=(
            "flowchart TB\n"
            "  Q[キュー先頭 v] --> P[popleft]\n"
            "  P --> L[各隣接 u]\n"
            "  L --> C{dist u == -1?}\n"
            "  C -- Yes --> S[\"dist[u] = dist[v]+1; append u\"]"
        )),
      S("Step 3: グリッド BFS",
        "迷路では (i, j) を 1 頂点、隣接は上下左右 4 方向。"
        "`dx, dy = [-1,0,1,0], [0,-1,0,1]` の定数を使うと書きやすい。"),
  ],
  pitfalls=(
      "- 必ず `deque.popleft()`。`list.pop(0)` は O(N) で TLE。\n"
      "- `visited` ではなく `dist == -1` で未訪問判定すると最短距離も同時に取れる。\n"
      "- 重み付きには使えない。それはダイクストラ (A64)。"
  ),
  related=["A52", "A62", "B63"])


L("A64",
  title="ダイクストラ法 — 重み付き最短路",
  tagline="未確定の中で最小距離の頂点から確定していく",
  intro=(
      "**非負重み** のグラフで、頂点 s から各頂点までの最短距離を求める。"
      "優先度付きキュー (ヒープ) で実装すると O((V+E) log V)。\n\n"
      "**手順**: 「未確定の中で距離最小の頂点」を取り出して確定 → その頂点から伸びる辺で隣の距離を更新。"
      "繰り返し。"
      "**負の重み** がある場合は使えない (Bellman-Ford や SPFA へ)。"
  ),
  sections=[
      S("Step 1: 初期化",
        "全頂点の距離 `cur` を INF に。`cur[s] = 0`。ヒープに `(0, s)` を入れる。",
        diagram=(
            "flowchart LR\n"
            "  S[start s] --> D[\"cur[s] = 0\"]\n"
            "  D --> H[\"heap = [(0, s)]\"]"
        )),
      S("Step 2: 取り出して確定",
        "ヒープから最小距離の頂点 v を取り出す。"
        "もう確定済みならスキップ。そうでなければ **その距離を確定**。",
        diagram=(
            "flowchart TB\n"
            "  H[ヒープから取り出し v] --> C{kakutei v?}\n"
            "  C -- Yes --> SK[スキップ]\n"
            "  C -- No --> K[\"kakutei[v] = True\"]"
        )),
      S("Step 3: 隣接頂点の更新",
        "v から伸びる各辺 (v→u, 重み w) について、"
        "`cur[u] > cur[v] + w` なら `cur[u]` を更新してヒープに push。",
        diagram=(
            "flowchart LR\n"
            "  V[v 確定] --> E[各辺 v→u]\n"
            "  E --> J{cur u > cur v + w?}\n"
            "  J -- Yes --> U[\"cur[u]= cur[v]+w; push\"]"
        )),
  ],
  pitfalls=(
      "- **負の重みでは動かない**。負があるなら Bellman-Ford。\n"
      "- ヒープに古い候補 `(古い距離, v)` が残るので、取り出したとき `kakutei[v]` チェックは必須。\n"
      "- 答えが INF なら **到達不能**。-1 などで出力。"
  ),
  related=["A53", "A61", "A73", "B64", "C14"])


L("A65",
  title="木上の DP — 葉から根へ集計",
  tagline="子の結果を集めて親へ伝える",
  intro=(
      "**木構造の上での DP**。各頂点 v について、「v を根とする部分木」に関する量を計算する。"
      "**葉から根に向かって計算** すれば、子の結果がすでに揃っているので親が計算できる。\n\n"
      "実装は DFS で帰りがけ順 (post-order) に値を集めるか、"
      "本問のように **「親 < 子」の入力性質を利用して i = N, N-1, ..., 1 の順** に走査する。"
  ),
  sections=[
      S("Step 1: 状態の定義",
        "**`dp[v] = v を根とする部分木に関する量`**。"
        "「部下数」「最大深さ」「最大独立集合のサイズ」など、問題ごとに定義。"),
      S("Step 2: 子から集める",
        "v の子 c について、`dp[v] += (dp[c] + 1)` のように **子の結果を集約**。"
        "「+1」は子自身を含める分。",
        diagram=(
            "flowchart TB\n"
            "  V[v 親] --> C1[子 c1]\n"
            "  V --> C2[子 c2]\n"
            "  C1 -->|dp c1 を集計| V\n"
            "  C2 -->|dp c2 を集計| V"
        )),
      S("Step 3: 走査順",
        "DFS で帰りがけ順に集めるのが王道だが、"
        "**入力の性質で「親 < 子」が保証されている** ときは **i = N..1 の単純ループ** で済む。"
        "今回はその好例。"),
  ],
  pitfalls=(
      "- 「子」と「親」の関係を入力から正しく構築する。**根はどれか?** を最初に決める。\n"
      "- 再帰 DFS で深い木は再帰上限に注意。`setrecursionlimit` または非再帰版。\n"
      "- 「+1」を入れ忘れて off-by-one になりがち。"
  ),
  related=["A62", "B65", "C16"])


L("A66",
  title="Union-Find — 連結成分の素集合データ構造",
  tagline="2 つの要素が同じグループか? 結合する。それだけ",
  intro=(
      "**「同じグループか?」「グループを併合する」** の 2 操作を **ほぼ O(1)** で行うデータ構造。"
      "**経路圧縮** と **Union by size/rank** を組み合わせれば、"
      "1 操作あたり O(α(N)) ≒ O(1) という驚異の速さ。\n\n"
      "MST (A67)、オフライン連結性 (B66)、グラフの連結成分など、多くの問題で土台となる。"
      "「辺を追加していくときの連結性」が得意。**辺の削除は苦手** (時系列を逆順にして追加に変換するのが定石)。"
  ),
  sections=[
      S("Step 1: 各要素の親を持つ",
        "`par[x] = x の親 (= 同じグループの代表)`。"
        "初期状態は `par[x] = -1` (各自が根)。",
        diagram=(
            "flowchart LR\n"
            "  P1[par 1 = -1] -.- G1[1 単独]\n"
            "  P2[par 2 = -1] -.- G2[2 単独]"
        )),
      S("Step 2: root(x) — 根を辿る",
        "`par[x] != -1` の間、親に進む。"
        "**経路圧縮**: 辿った頂点を全部 **根に直結** して、次回以降高速化。",
        diagram=(
            "flowchart TB\n"
            "  X[x] --> P[par x]\n"
            "  P --> P2[par par x]\n"
            "  P2 --> R[根]\n"
            "  X -.経路圧縮で直接.-> R"
        )),
      S("Step 3: unite(u, v) — 結合",
        "`root(u)` と `root(v)` が違うなら、**小さい木を大きい木に繋ぐ** (union by size)。"
        "サイズが揃うと木が深くならない。"),
  ],
  pitfalls=(
      "- 経路圧縮なしの素朴版は **最悪 O(N)**。両方の最適化を必ず入れる。\n"
      "- 辺削除には弱い。**オフライン + 時系列逆順 + 追加** に書き換えるのが王道。\n"
      "- 配列サイズ N+1 にして 1-indexed に揃えると問題と一致して読みやすい。"
  ),
  related=["A67", "B66", "C20"])


L("A67",
  title="MST — クラスカル法",
  tagline="辺を小さい順にソート + Union-Find で連結化",
  intro=(
      "**最小全域木 (MST)** = 全頂点を繋ぐ部分グラフのうち辺重み和が最小のもの。"
      "**クラスカル法**: 辺を重み昇順にソートし、両端が違うグループなら採用。"
      "Union-Find (A66) で効率化して O(M log M)。\n\n"
      "「最大全域木」は降順にすれば同じ手順で求まる (B67)。"
      "プリム法 (ヒープベース) という別解もあるが、Python ではクラスカルが書きやすい。"
  ),
  sections=[
      S("Step 1: 辺を重みでソート",
        "`edges.sort(key=lambda e: e[2])` で重み昇順。",
        diagram=(
            "flowchart LR\n"
            "  E[全辺] --> S[重み昇順ソート]"
        )),
      S("Step 2: 左から見て採用判定",
        "辺 (a, b, c) について、`a` と `b` が **違うグループ** なら採用。"
        "Union-Find で `same(a, b)` を判定し、`unite(a, b)` で結合。",
        diagram=(
            "flowchart TB\n"
            "  E[辺 a b c] --> J{same a b?}\n"
            "  J -- No --> A[\"答えに c を加算; unite a b\"]\n"
            "  J -- Yes --> SK[スキップ]"
        )),
      S("Step 3: なぜ最小か (= 交換論法)",
        "ある最適 MST T と、クラスカルの結果 K が違うとする。"
        "K が選んで T が選ばなかった最初の辺 e を考えると、"
        "T に e を加えてサイクル中で重い辺と入れ替えると、より良いか同等の MST が作れる → 矛盾。"),
  ],
  pitfalls=(
      "- 連結グラフでない場合、選んだ辺の本数が N-1 にならない (= MST が存在しない)。\n"
      "- 最大全域木は **降順** ソートに変えるだけ (B67)。\n"
      "- 同じ重みの辺が複数あるときの結果は一意でない (どれを採用しても OK)。"
  ),
  related=["A66", "B67"])


L("A68",
  title="最大フロー — Ford-Fulkerson 法",
  tagline="s から t まで増加路を見つけて流せるだけ流す",
  intro=(
      "ネットワークフロー: 始点 s からシンク t まで、各辺の **容量** を超えない範囲で"
      "流せる最大流量を求める。\n\n"
      "**Ford-Fulkerson**: DFS で「まだ流せる路 (増加路)」を見つけ、流せるだけ流す。"
      "増加路がなくなったら終了。"
      "**残余グラフと逆辺** の概念がカギ。逆辺は **「流れを取り消せる余地」** を表す。"
  ),
  sections=[
      S("Step 1: 残余グラフの構築",
        "各辺 (u→v, 容量 c) に対し、**逆辺 (v→u, 容量 0)** をペアで追加。"
        "後で流れを差し戻せるようにするための準備。",
        diagram=(
            "flowchart LR\n"
            "  F[\"u→v 容量 c\"] --> G[グラフ]\n"
            "  B[\"v→u 容量 0 (逆辺)\"] --> G"
        )),
      S("Step 2: 増加路探索",
        "DFS で s → t のパスを探す。**残容量 > 0 の辺だけ通る**。"
        "見つかったら、パスの最小容量だけ流す。",
        diagram=(
            "flowchart LR\n"
            "  S[s] --> A[a] --> B[b] --> T[t]\n"
            "  S -.最小容量 F.- T"
        )),
      S("Step 3: 残余の更新",
        "流した量 F を、順辺から引き、**逆辺に足す**。"
        "逆辺が増えると「もし良ければ後で取り消せる」状態になり、最適性が担保される。",
        diagram=(
            "flowchart LR\n"
            "  N[\"順辺 -= F\"]\n"
            "  R[\"逆辺 += F\"]"
        )),
  ],
  pitfalls=(
      "- 逆辺を入れ忘れると **最適でない**。Ford-Fulkerson の本質。\n"
      "- 計算量は O(F·E)。F が大きいと遅い。**Dinic 法** (BFS で短い path から) ならもっと速い。\n"
      "- 容量が浮動小数のときは収束しないことがある。整数限定が安全。"
  ),
  related=["A69", "B68"])


L("A69",
  title="二部マッチング — 最大フローへの帰着",
  tagline="左右の頂点を二部分割し、s と t を加えて最大フロー",
  intro=(
      "**二部グラフのマッチング数 (最大ペア数)** を求める問題は、"
      "**最大フロー (A68) に帰着** して解ける。\n\n"
      "左頂点群 → 右頂点群 への辺、ソース s → 各左、各右 → シンク t、"
      "**すべての辺の容量を 1** にして、s から t への最大フローを求めれば最大マッチング数。"
  ),
  sections=[
      S("Step 1: グラフの構築",
        "- s → 各左頂点 (容量 1)\n"
        "- 各左頂点 → マッチ可能な右頂点 (容量 1)\n"
        "- 各右頂点 → t (容量 1)",
        diagram=(
            "flowchart LR\n"
            "  S[s] --> L1[左1]\n"
            "  S --> L2[左2]\n"
            "  L1 --> R1[右1]\n"
            "  L2 --> R1\n"
            "  L2 --> R2[右2]\n"
            "  R1 --> T[t]\n"
            "  R2 --> T"
        )),
      S("Step 2: 容量 1 がポイント",
        "**各頂点を 1 回しか使えない** ことを「容量 1」で表現。"
        "結果として 1 単位の流れが 1 つのマッチに対応する。"),
      S("Step 3: 最大フローを計算",
        "Ford-Fulkerson などで `s → t` の最大フローを求める。"
        "答え = フロー値 = マッチング数。"),
  ],
  pitfalls=(
      "- 「最小辺被覆」「最大独立集合」など、二部グラフの双対問題に対応 (König の定理)。\n"
      "- 容量 1 のフロー問題は **Hopcroft-Karp** で O(E·√V) に高速化可能。\n"
      "- 通常の Ford-Fulkerson でも O(V·E) で動く。"
  ),
  related=["A68", "B69"])


L("A70",
  title="状態 BFS — 2^N 通りの状態を頂点に持つ",
  tagline="集合を整数で表し、状態空間グラフを BFS",
  intro=(
      "「ランプの ON/OFF」「マスの色」のような **N ビットの状態** を、整数 0..2^N-1 で表現。"
      "**状態を頂点、1 回の操作を辺とするグラフ** を構築し、BFS で最短手数を求める。\n\n"
      "N = 20 でも状態数 10^6 程度で現実的。"
      "**bitDP (A23) と発想は同じ** で、両者は親戚のような関係。"
  ),
  sections=[
      S("Step 1: 状態を整数化",
        "ランプ N 個の状態は `state = Σ (lamp[i] << i)` のような N ビット整数。"
        "状態数は 2^N。",
        diagram=(
            "flowchart LR\n"
            "  L[\"ランプ [1, 0, 1, 1]\"] --> S[\"state = 13 = 1011_2\"]"
        )),
      S("Step 2: 状態遷移グラフを構築",
        "各状態から、各操作で行ける次状態を計算。"
        "「ランプ x, y, z を反転」なら **XOR** で `next = state ^ (1<<x) ^ (1<<y) ^ (1<<z)`。",
        diagram=(
            "flowchart LR\n"
            "  S[state] --> O1[操作 1: XOR]\n"
            "  O1 --> N1[次状態 1]\n"
            "  S --> O2[操作 2: XOR]\n"
            "  O2 --> N2[次状態 2]"
        )),
      S("Step 3: BFS で最短",
        "初期状態 (= 入力されたランプ配置) からゴール (= 全 1) までの BFS。"
        "距離が答え。"),
  ],
  pitfalls=(
      "- 状態数 2^N がメモリに乗るか確認。N = 25 でも 33M で厳しい。\n"
      "- XOR で集合反転は強力。`s ^ mask` で複数 bit を同時反転できる (A70_extra)。\n"
      "- 「最短」が問題なら BFS、「到達可能性」だけなら DFS でも可。"
  ),
  related=["A23", "A63"])


# =====================================================================
#  Chapter 10 — 総合問題・力試し (A71〜A77)
# =====================================================================

L("A71",
  title="ソート + 貪欲 — 並べ替えで答えが決まる典型",
  tagline="2 列の積和を最小化 = 昇順 × 降順",
  intro=(
      "「2 つの数列 A, B から 1 つずつ取って積を作り、合計を最小化」"
      "といった問題で、**「A は昇順、B は降順に並べて掛ける」** のが必ず最適。\n\n"
      "直感的には「大きいものは小さいものと組ませる」のが効率的。"
      "厳密には **再配置不等式** で証明される、基本的だが応用範囲の広い貪欲。"
  ),
  sections=[
      S("Step 1: 並べ替える",
        "A は昇順、B は降順 (またはどちらか片方ずつ) にソート。",
        diagram=(
            "flowchart LR\n"
            "  A[\"A: 大きい順\"] --> P[ペア化]\n"
            "  B[\"B: 小さい順\"] --> P\n"
            "  P --> Q[積を計算]"
        )),
      S("Step 2: 同じインデックスで積",
        "ソート後の `A[i] × B[i]` を全 i について計算し合計する。",
        diagram=(
            "flowchart LR\n"
            "  A1[A 1] --> X[\"×\"]\n"
            "  B1[B 1 = 降順 1 番目] --> X\n"
            "  X --> S[合計]"
        )),
      S("Step 3: 別バリエーション",
        "- 最大化なら **両方とも昇順 (or 両方とも降順)** が最適。\n"
        "- 「ペアにできない」制約があるときは別の DP が必要 (A75)。"),
  ],
  pitfalls=(
      "- 並びの方向を間違える (最小化なのに同方向にソート) と簡単に WA。\n"
      "- 数値型のオーバーフロー (Python は不要、C++ は注意)。\n"
      "- 「貪欲が最適」を証明できない問題には使わない (反例を作って確認するクセを)。"
  ),
  related=["A39"])


L("A72",
  title="ビット全探索 + 貪欲 — 一方を 2^N で網羅、他方を貪欲",
  tagline="行は全探索、列は貪欲。組み合わせて最適化",
  intro=(
      "「行と列の両方に対して操作を選ぶ」ような 2 軸最適化問題で、"
      "**片方 (例えば行) を 2^H 通り全探索し、他方 (列) を貪欲** に決める手法。\n\n"
      "全探索なので最適性が保証される一方、列を全探索する必要がないので計算量を抑えられる。"
      "H ≤ 20 程度が現実的な上限。"
  ),
  sections=[
      S("Step 1: 行のビット全探索",
        "行 H 個の選び方を 2^H 通り全列挙。"
        "`itertools.product([0, 1], repeat=H)` か `for mask in range(1 << H)`。",
        diagram=(
            "flowchart LR\n"
            "  H[行 H 個] --> M[2^H 通り]\n"
            "  M --> S[各組み合わせを試す]"
        )),
      S("Step 2: 各組み合わせで列を貪欲決定",
        "行の使い方を固定したら、残り操作回数で **列ごとに白マス数が多い順** に塗る。",
        diagram=(
            "flowchart TB\n"
            "  F[行の使い方を固定] --> R[残り操作回数 K']\n"
            "  R --> C[各列の白マス数を計算]\n"
            "  C --> SO[降順ソート]\n"
            "  SO --> A[上位 K' 個を塗る]"
        )),
      S("Step 3: 全パターンの max",
        "全パターンについて答えを計算し、max を取る。\n\n"
        "計算量: O(2^H × (H + W log W))。"),
  ],
  pitfalls=(
      "- H = 20 が現実的な上限。H が大きいなら別アプローチ。\n"
      "- 列の貪欲で「降順ソート」を忘れない。\n"
      "- 残り操作回数が負になるパターンはスキップ。"
  ),
  related=["A03", "A23", "A42"])


L("A73",
  title="2 目的のダイクストラ — 重みに 10000 倍を仕込む",
  tagline="第一基準 × 10000 + 第二基準。1 本に集約",
  intro=(
      "「距離を最小化したい、同じ距離なら木の本数も最少化したい」"
      "のような **2 つの最適化基準** を、"
      "**重みに `10000` を仕込む** ことで 1 つのダイクストラに集約するテクニック。\n\n"
      "新しい重み = (第一基準) × 10000 + (第二基準)。"
      "第一基準が支配的で、同点の場合だけ第二基準が効く。"
      "10000 は「第二基準の最大値 + α」で十分。"
  ),
  sections=[
      S("Step 1: 重みの設計",
        "各辺の重みを **(距離) × 10000 − (木の本数だけ −1)** に変換。"
        "距離が支配的で、同距離内では木の本数が少ない方が小さい値になる。",
        diagram=(
            "flowchart LR\n"
            "  E[\"辺 c, d\"] --> N[\"新重み = 10000c - (d == 1 ? 1 : 0)\"]"
        )),
      S("Step 2: 普通のダイクストラ",
        "A64 と同じ実装。重みが変わっているだけ。"
        "答えは `cur[N]`。"),
      S("Step 3: 結果の解読",
        "**`distance = (cur[N] + 9999) // 10000`** (切り上げ)。\n"
        "**`num_trees = distance × 10000 - cur[N]`** (差分)。",
        diagram=(
            "flowchart LR\n"
            "  C[\"cur[N]\"] --> D[\"distance = ceil(/10000)\"]\n"
            "  C --> T[\"num_trees = d*10000 - cur\"]"
        )),
  ],
  pitfalls=(
      "- 10000 は **第二基準の取りうる値の最大 + 1 以上** にする。"
      "  本数が 10000 を超える問題では 10^5 や 10^6 に。\n"
      "- 整数オーバーフローに注意 (Python なら平気)。\n"
      "- 同点処理の方向 (max / min) を間違えると符号がズレる。"
  ),
  related=["A64", "B64"])


L("A74",
  title="転倒数分解 — スライドパズルを行・列に分ける",
  tagline="2 次元の問題を 2 つの 1 次元問題に",
  intro=(
      "スライドパズル: N×N 盤面の数を目標配置にする最小スワップ数。"
      "操作が **行内 / 列内に閉じている** とき、**行の並べ替えと列の並べ替えに独立分解** できる。\n\n"
      "各方向の操作回数 = **転倒数** (i < j で A[i] > A[j] となる組数)。"
      "1 次元 2 つの転倒数の和が全体の答え。"
  ),
  sections=[
      S("Step 1: 行・列に分解",
        "盤面から、行 X[i] と列 Y[j] の代表値を抽出。"
        "「行同士の並べ替えと列同士の並べ替えに独立に分解できる」性質を使う。",
        diagram=(
            "flowchart LR\n"
            "  G[2 次元盤面] --> X[行の代表値 X]\n"
            "  G --> Y[列の代表値 Y]"
        )),
      S("Step 2: 各列の転倒数を計算",
        "`X` と `Y` のそれぞれについて、転倒数を素朴な二重ループで計算 (N が小さければ O(N²) で OK)。"
        "大きいなら **マージソート / BIT** で O(N log N)。",
        diagram=(
            "flowchart LR\n"
            "  X[X] --> IX[転倒数 X]\n"
            "  Y[Y] --> IY[転倒数 Y]"
        )),
      S("Step 3: 和が答え",
        "**`answer = 転倒数(X) + 転倒数(Y)`**。\n"
        "操作が独立だから単純な和になる。"),
  ],
  pitfalls=(
      "- 操作が行内・列内に閉じているという前提を確認 (一般のスライドパズルには使えない)。\n"
      "- 転倒数の効率計算 = BIT (Fenwick) + 座標圧縮 (A15) のコンボ。\n"
      "- 0 マスがある場合、扱いを正しく決める。"
  ),
  related=["A15", "B59"])


L("A75",
  title="締切付きジョブ DP — ソート + DP",
  tagline="締切順に並べてから DP",
  intro=(
      "「各ジョブに所要時間 T と締切 D がある。解けるジョブ数を最大化」"
      "という古典問題。**まず締切で昇順ソート、それから DP** が定石。\n\n"
      "ソートしない素朴な DP では「順番」も状態に入れる必要があり爆発するが、"
      "ソートすれば順番が固定され、**`dp[i][j] = i 個目までで時刻 j のときの最大解ける数`** で済む。"
  ),
  sections=[
      S("Step 1: 締切で昇順ソート",
        "「締切が早いものから処理を考える」ように並べておく。"
        "貪欲ではなく **DP のための前処理** であることに注意。",
        diagram=(
            "flowchart LR\n"
            "  J[ジョブ群] --> S[締切順ソート]"
        )),
      S("Step 2: DP の遷移",
        "**`dp[i+1][j] = max(dp[i][j], dp[i][j-t] + 1)`** (時刻 j に i を解くなら)。\n"
        "条件は `t ≤ j ≤ d` (時刻 j に間に合い、ジョブ i の所要時間が j 以下)。",
        diagram=(
            "flowchart LR\n"
            "  D1[\"dp[i][j] (i を解かない)\"] --> M[max]\n"
            "  D2[\"dp[i][j-t] + 1 (i を解く)\"] --> M\n"
            "  M --> D[\"dp[i+1][j]\"]"
        )),
      S("Step 3: 答えの取り出し",
        "**`max(dp[N])`** = どの時刻でもいいので最大解けるジョブ数。"),
  ],
  pitfalls=(
      "- ソートを忘れると遷移が壊れる。前処理が本質。\n"
      "- 時刻の最大値 (MAX_D) が大きいとメモリ・計算量が辛い。\n"
      "- 「給料最大化」など別最適化なら別 DP が必要 (B39)。"
  ),
  related=["A19", "A39"])


L("A76",
  title="区間 DP の高速化 — 累積和で遷移を O(1) に",
  tagline="DP 内の `Σ` を累積和で潰す典型",
  intro=(
      "区間 DP の遷移に **「区間内の和」** が出てくるとき、"
      "累積和 (A06) を一緒に持っておくと 1 つの遷移が O(1) になる。\n\n"
      "全体 O(N²) の DP の中の各遷移が O(N) なら全体 O(N³)、O(1) に潰せば O(N²)。"
      "「DP + 累積和」「DP + セグメント木」など、**DP の遷移を別データ構造で加速** するパターンは応用範囲が広い。"
  ),
  sections=[
      S("Step 1: DP 遷移内の Σ を見つける",
        "`dp[i] = max_k (dp[k] + Σ_{j=k}^{i} ...)` のような形で、"
        "和 Σ が遷移内に毎回出てくるなら **累積和の出番**。",
        diagram=(
            "flowchart LR\n"
            "  D[\"dp[i] の遷移\"] --> S[\"Σ が含まれる?\"]\n"
            "  S -- Yes --> A[累積和で O 1 化]"
        )),
      S("Step 2: 累積和を前計算",
        "Σ の対象配列について累積和 `S[i]` を計算。"
        "**`Σ_{j=l}^{r} A[j] = S[r] - S[l-1]`** で O(1) に。"),
      S("Step 3: DP に組み込む",
        "DP の遷移を累積和を使って書き直す。"
        "二重ループの全体は変わらないが、内側の Σ が消えて O(N²) に。",
        diagram=(
            "flowchart LR\n"
            "  D[\"DP O(N²)\"] --> T[各遷移 O 1]\n"
            "  T --> R[全体 O N²]"
        )),
  ],
  pitfalls=(
      "- 累積和の範囲を間違えると遷移がズレる。**手で 1 ステップ確認** する。\n"
      "- 累積和ではなく **累積 max / min** が必要なケースも (セグ木が必要になる)。\n"
      "- DP 自体の状態数を減らせないか? も一緒に検討。"
  ),
  related=["A06", "A21"])


L("A77",
  title="切り方の最大化 — 「最小区間長 ≥ x」で二分探索",
  tagline="A12 と同じ枠組み。OK/NG の境目を最大化",
  intro=(
      "「N 個の印から K 個選んで棒を切り、できる N+1 個の区間の最小長を最大化」"
      "という古典問題。**「最小区間長 ≥ x にできるか?」** という判定問題に書き換えれば、"
      "x について **NG → OK が単調** に変わるので二分探索。\n\n"
      "判定は **左から見て「直前の切れ目から x 以上離れていれば切る」** の貪欲。"
      "判定 + 二分探索 = 「答えで二分探索」(A12) の典型パターン。"
  ),
  sections=[
      S("Step 1: 判定関数 check(x)",
        "「最小区間長 ≥ x にできるか?」を判定。"
        "**左から貪欲**: `last_kireme` から x 以上離れていれば切る、を繰り返す。\n"
        "切れ目の数が K 以上なら OK。",
        diagram=(
            "flowchart TB\n"
            "  S[直前の切れ目 last] --> I[次の印 A i]\n"
            "  I --> C{A i - last >= x AND L - A i >= x?}\n"
            "  C -- Yes --> A[切る; last = A i; cnt += 1]\n"
            "  C -- No --> SK[スキップ]"
        )),
      S("Step 2: 二分探索の方向",
        "答えを最大化したいので **切り上げ版** の二分探索を使う。\n\n"
        "```python\n"
        "while left < right:\n"
        "    mid = (left + right + 1) // 2  # 切り上げ\n"
        "    if check(mid):\n"
        "        left = mid\n"
        "    else:\n"
        "        right = mid - 1\n"
        "```"),
      S("Step 3: 範囲",
        "`left = 1, right = L`。"
        "「答え x は 1 〜 L の間にある」と分かっているので、その範囲で二分。"),
  ],
  pitfalls=(
      "- 最大化のときは **`mid = (l + r + 1) // 2`** で切り上げ。"
      "  切り上げ忘れで無限ループ。\n"
      "- 「最小値を最大化」「最大値を最小化」は両方とも答えで二分探索の出番。\n"
      "- 判定関数 `check(x)` の貪欲が本当に正しいか手で確認。"
  ),
  related=["A12"])


# 法則 ID で並べたリスト (1〜77 全部揃っているか確認用)
EXPECTED_LAWS = [f"A{i:02d}" for i in range(1, 78)]
assert set(LAWS.keys()) == set(EXPECTED_LAWS), \
    f"Missing laws: {set(EXPECTED_LAWS) - set(LAWS.keys())}"
